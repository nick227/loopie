# Project State — LOOPIE

## MVP

**What it does:** A business manages owned communication (email/text/social messages), paid advertising campaigns, LOOPIE-hosted landing pages, first-party ad serving, and forms from one platform, with every lead and sale — regardless of which side generated it — landing in one shared CRM pipeline.

**Users:**
- Single role in V1: Business user (owner/manager/marketer). No MNC-staff-on-behalf-of-client portal, no agency/multi-account layer.

**In V1:**
- Contacts & Audiences — import/create contacts, channel eligibility/consent, segment into audiences (predefined + custom filters)
- Messages — compose/send email & text to an audience, save/reuse templates, optional automatic follow-up (max 2 steps, source-agnostic `LEAD_CREATED` trigger)
- Advertising — create a campaign (creative + platform + budget + dates), Deployment record per platform, tracked attribution redirect → landing page or lead capture on form submit
- **Landing Pages** — template-driven (not freeform) authoring: `LandingPageTemplate → LandingPage (draft) → PublishedPageVersion (immutable)`. LOOPIE-hosted at `/p/{slug}`, downloadable HTML export, draft/published lifecycle.
- **Forms** — first-class reusable `Form`/`FormField`, referenced by LandingPage. Submission is the canonical identity transition: anonymous session → `FormSubmission` → resolve/create Contact → create Lead, preserving Deployment/AdUnit/UTM attribution.
- **First-party advertising (AdUnit)** — a genuinely separate delivery mechanism from `Deployment` (external platforms). Served by `apps/ad-server`, a standalone deployable service sharing the same database. Feeds the same Lead/Sale spine via `sourceType = AD_UNIT`.
- Leads & Sales — one pipeline (NEW → CONTACTED → QUALIFIED → QUOTED → WON/LOST), Sale recording, attributed back to `sourceType`/`sourceMessageId`/`sourceDeploymentId`/`sourceAdUnitId` regardless of origin
- Results — Home (operational: what needs attention today) plus per-surface Performance (Campaigns → Performance, Messages → Performance); no standalone "Results"/"Reports" nav surface — see the 2026-08-27 IA revision below

**Parking lot (V2+):**
- Live Meta/Google/TikTok API connectors — V1 stores real `Deployment` records but platform status/spend/metrics are entered manually via `PATCH /deployments/{id}`, not synced live.
- Social post publishing to live platforms (V1 = compose/draft only).
- Branching/visual automation builder, >2-step sequences.
- Agency/multi-account/white-label management.
- Billing/subscriptions, deep role/permission systems.
- AI-generated content.
- Drag-and-drop landing-page builder (V1 is intentionally template-constrained — see below).
- Custom-domain DNS/cert provisioning for landing pages (the `customDomain` field exists; nothing automates it yet).
- A "Formerly" external landing-page template catalog/import flow (the data model — `LandingPageTemplate.businessId` nullable, `formatVersion` — is shaped for this, but no import mechanism exists).
- Automation *execution* — no background scheduler evaluates `waitDays`/`condition` and fires actions. Rules are stored and inspectable, not run. (Carried over from Phase 2 — still true.)

**Stack:** factory defaults (Vite + React, Fastify, Prisma + MySQL) — no overrides. Two Fastify services now: `apps/server` (contract-first, everything account/business-facing) and `apps/ad-server` (plain routes, public first-party ad serving only).

**Plugins:** file-upload (Asset library), transactional email + SMS send (Messages channel delivery) — not yet installed; to confirm against `references/plugin-catalog.md` before Phase 4/5. Ad-platform OAuth connectors deferred to V2. Railway deploy config exists for `apps/ad-server` only (`apps/ad-server/Dockerfile` + `railway.json`) — `apps/server` doesn't have one yet, see the `railway` plugin.

**Source docs:** `docs/00-unified-data-model.md` (canonical data model), `docs/00-unified-ia-navigation.md` (canonical IA/nav — **revised 2026-08-27**: `Home · Campaigns · Messages`, replacing the earlier `Home · Contacts · Messages · Advertising · Results`; "Campaigns" is the literal top-level label again, Contacts moved under Messages, Results/Reports dropped as a nav concept entirely in favor of per-surface Performance), plus the Messaging POC and Campaigns doc packs for UX/behavior detail per surface.

## Phase Completed

Phase 1 — Contract (done). Phase 2 — Server (done). Acquisition-chain extension — landing pages, forms, first-party ad serving (done, **live-DB verified**). Phase 3 — Frontend shell (done, **live-DB + real-browser verified** — see below). Phase 4 (feature pages) starts next; its domain-hooks step (19) is already done, pulled forward because Phase 3's own gate needed a working Home page.

## Modules Built

- [x] Monorepo scaffold (pnpm workspaces, root scripts, CI, SDK/DB/api-spec packages)
- [x] Prisma schema — 30 models. Core spine plus: `LandingPageTemplate`, `LandingPage`, `PublishedPageVersion`, `PageView`, `Form`, `FormField`, `FormSubmission`, `AdUnit`. `SourceType` extended with `AD_UNIT`; `Lead`/`Sale`/`Interaction` extended with `sourceAdUnitId`; `Deployment` extended with `destinationLandingPageId`; `Platform` extended with `LOOPIE`. Compiles (`prisma generate` succeeds) and **pushed clean to a live database**.
- [x] OpenAPI spec — 91 operations, 0 Redocly errors. Tags: auth, contacts, audiences, assets, templates, creatives, messages, automations, campaigns, `landing-pages` (14 ops), `forms` (5 ops), `ad-units` (4 ops, CRUD only), attribution, leads, sales, dashboard.
- [x] SDK — client + generated types + hooks for **every** domain (`useAuth`, `useContacts`, `useAudiences`, `useAssets`, `useTemplates`, `useCreatives`, `useMessages`, `useAutomations`, `useCampaigns` incl. deployments, `useLandingPages` incl. templates, `useForms`, `useAdUnits`, `useLeads`, `useSales`, `useDashboard`). `packages/sdk` typechecks clean.
- [x] `apps/server` — every operationId has a handler+service, including `LandingPageTemplateService`, `LandingPageService` (CRUD, publish/versions, export-to-HTML, hosted serve at `/p/{slug}`, the canonical submission flow), `FormService`, `AdUnitService` (CRUD only — serving lives in ad-server). `AttributionService` shares `lib/identityResolution.ts` with `LandingPageService.submit`. `CampaignService.performance()` / `DashboardService.results()` roll up landing-page and first-party ad-unit metrics alongside external Deployments.
- [x] `apps/ad-server` — standalone Fastify service (not contract-first — see its README). Routes: `/health`, `/serve/:adUnitId`, `/embed/:adUnitId`, `/impression/:adUnitId`, `/click/:adUnitId`. Own `package.json`, `tsconfig.json`, `Dockerfile`, `railway.json`.
- [x] Tests — stubs for all 91 operations, plus real integration suites: `apps/server/src/__tests__/acquisitionPath.test.ts` (2 tests) and `apps/ad-server/src/__tests__/adServe.test.ts` (4 tests). **Both pass 100% against a live database.**
- [x] `apps/web` — Vite + React + Tailwind scaffold (built by hand, not `pnpm create vite` — see Deviations), all UI primitives from `references/frontend-design.md`, `Shell.tsx` with the real nav (**Home · Campaigns · Messages**, revised 2026-08-27 — see Deviations), dark mode wired, 76 page stubs + `App.tsx` generated from the spec, Playwright smoke test. **3/3 Playwright tests pass in a real headless browser against the live server + live DB**: login → Home → logout; register; unauthenticated redirect.
- [ ] Feature pages — Phase 4. The 76 generated pages are stubs (`JSON.stringify` placeholders); ~25 Create/Update stub pages also have a naive-Zod-inference typecheck gap (see "Known Gap" below) that real forms will resolve as a side effect of being written properly.
- [ ] Admin panel (not needed — no admin role in V1 scope)
- [ ] Polish (Phase 5)
- [ ] Documentation (Phase 6)

## Known Gap: Generated Stub Pages Have Naive Field Types

`scripts/generate-pages.ts`'s Zod-schema inference (`buildZodExpr`) only ever produces `z.string()` — it doesn't read the OpenAPI property's actual `type`/`enum`/array-ness. Every generated Create/Update page's form data is therefore all-strings, which doesn't match the correctly-typed domain hooks (real enums, `number`, `string[]`, `Record<string, unknown>`). This is a **pre-existing, systemic generator limitation** — confirmed present for the original Contacts/Audiences pages too, not introduced by this session's hook-writing. `apps/web` typechecks clean except for ~25 of these stub pages (`Create*Page.tsx`/`Update*Page.tsx`, plus the handful of parameterless action stubs like `PauseCampaignPage.tsx` that never actually capture the id they'd need to act on — also a stub-content issue, not a hook issue). Fixing the generator's type inference and/or hand-writing correct forms is Phase 4 work by design ("replace the JSON.stringify placeholders with real component renders").

Two *generator* bugs were fixed for real, not left as gaps: `detectPattern` misclassified parameterless singleton GETs (`/home`, `/results`) as collection-list pages that called `.map()` on a plain object; `genDetailPage` now handles the no-path-param case (no `useParams`, hook called with no args) instead of assuming every detail page has an id.

## Live Database: Verified

A real `DATABASE_URL` (`mysql://loopie:loopie_dev_password@localhost:3306/loopie` — a pre-provisioned MariaDB user/db on this machine, not created by this session) replaced the earlier "no provisioning access" blocker. Full path exercised for real:

- `pnpm db:push` — the entire 30-model schema, including the LandingPage↔PublishedPageVersion cycle, synced clean on the first try.
- `pnpm db:seed` — creates a full realistic dataset (business, contact, lead, message, campaign, deployment, landing page + published version, form, ad unit) with no errors.
- `pnpm --filter server test` and `pnpm --filter ad-server test`, both against the live DB. The two hand-written integration suites with real assertions — `acquisitionPath.test.ts` (2 tests) and `adServe.test.ts` (4 tests) — **pass 100%**. The 91 auto-generated stub tests are ~59% passing; every remaining failure is a plain 404 (placeholder UUID path param, no seeded resource) or 500 (payload deliberately left as `// payload: {},`, never filled in) — exactly the documented, expected state of unfilled stubs (see `references/testing-patterns.md` — filling them in is explicitly a separate developer task, not something this pass was asked to do).
- Manual curl smoke test end-to-end: login → session cookie → `/home` and `/results` (correctly blending Deployment + AdUnit impressions: 42,100 + 3,200 = 45,300) → hosted `/p/raw-customer-stories` renders real HTML with the real form → tracked click at `/r/demo-deployment-meta` redirects correctly → form submission creates a real attributed Contact + Lead → `/results` and campaign `/performance` both update correctly afterward.

**One real bug found and fixed by this verification, not by inspection:** the click redirect (`AttributionService.trackClick` in `apps/server`, `AdServeService.recordClick` in `apps/ad-server`) never appended the session id to the redirect URL, and the rendered landing page's form-submit script referenced an undefined `window.__loopieSid` instead of reading `sid` from its own URL. Manually querying the DB for the session id made the flow *look* correct in isolation, but a real browser visitor had no way to know it — the click→page→submission chain was silently unlinked in practice. Fixed in both services (`withSid()` helper appends `?sid=` to any absolute redirect URL) and in `lib/renderLandingPage.ts` (form script now reads `sid` from `location.search`). Both integration test suites were strengthened to assert this explicitly (parsing the actual redirect `Location` header rather than reading the session id back out of the database) and still pass.

**One operational note:** the test suites' `afterEach` wipes every table in the connected database (by design, per `references/testing-patterns.md`) — running them against the same DB used for seeded demo data destroys that data. Re-run `pnpm db:seed` after running tests if you need the demo data back; a separate test database is the real fix if this becomes a recurring workflow.

## Deviations from Defaults

- **OpenAPI version pinned to 3.0.3, not 3.1.0.** See prior reasoning — `nullable: true` throughout is invalid OpenAPI 3.1.
- **SDK hooks never destructure `{ data, error, response }` off an awaited `client.METHOD(...)` call, and never narrow directly on `result.error` inside an `if`.** TS 5.9.3 + openapi-fetch 0.12.5 collapses inference to `never` otherwise — verified in isolation. See `packages/sdk/src/hooks/useAuth.ts`.
- **`RegisterInput` uses `businessName`, not `username`/`displayName`.**
- **Business/User split instead of User/Profile split.**
- **Source is a discriminant (`sourceType`) + typed nullable FKs** — now three: `sourceMessageId`, `sourceDeploymentId`, `sourceAdUnitId`. Same reasoning as before, extended.
- **`apps/ad-server` does not use `fastify-openapi-glue` / the shared OpenAPI spec.** Its four routes are public, high-volume, and consumed by embed snippets and tracking pixels, not the LOOPIE web app's typed SDK — a second contract-first pipeline for four routes would be machinery without a benefiting consumer. See `apps/ad-server/README.md`.
- **`AttributionEvent.deploymentId`/`adUnitId`/`campaignId`/`creativeId`/`platform` are all nullable now** (were required). Either `deploymentId` xor `adUnitId` is set, enforced in application code (both `AttributionService.trackClick` and `AdServeService.recordClick`), not a DB constraint — Prisma/MySQL has no clean way to express "exactly one of two nullable FKs" declaratively.
- **`LandingPage.slug` is globally unique**, not per-business — supports clean `/p/{slug}` hosted URLs without a business-id prefix, and sets up custom-domain mapping later. Enforced at the service layer (409 on conflict) and via `@unique` in schema.
- **`Form` structure is not frozen into `PublishedPageVersion`.** Only `content`/`theme`/`formId` (a pointer) are snapshotted on publish. Editing a Form's fields after publishing changes what a live page renders. This is intentional — Forms are reusable, live entities by design (see the brief: "first-class reusable entities rather than embedded JSON") — but it means "immutable published version" covers page content, not form structure. Documented in `schema.prisma` and the `updateForm` OpenAPI description.
- **Landing-page performance's `leads`/`sales` are computed by joining through `FormSubmission.leadId`**, not a direct `landingPageId` FK on `Lead`/`Sale`. Keeps the Source pattern exactly as it was (no new FK sprawl on the core spine) at the cost of an extra join — acceptable at V1 scale.
- **AdUnit has no `spend` field.** First-party inventory has no external-platform cost; `byPlatform`/`bySource` rows for `LOOPIE`/`AD_UNIT` always report `spend: null`/`0`.
- **`packages/db/tsconfig.json`** now includes `prisma` (previously only `src`), and `bcryptjs`/`@types/bcryptjs` moved to `packages/db`'s own dependencies (were only in `apps/server`'s) — `prisma/seed.ts` uses bcrypt directly and was silently unchecked before this pass.
- **`apps/server/vitest.config.ts` sets `fileParallelism: false`.** Found live: Vitest's default parallel-file execution against one shared real database, combined with `setup.ts`'s global `afterEach` table wipe, caused test files to delete each other's mid-test data — surfaced as spurious 401s that had nothing to do with auth. Serial file execution fixed it; the underlying cause (one shared DB, no per-file isolation) is worth revisiting if the suite grows large enough for serial execution to matter.
- **Click redirects append `?sid=<sessionId>`** to any absolute destination URL — a `withSid()` helper in `apps/server/src/services/AttributionService.ts`, duplicated (not shared, different packages) in `apps/ad-server/src/services/AdServeService.ts`. Found live, not by inspection — see "Live Database: Verified" above.
- **`apps/web` was scaffolded by hand, not `pnpm create vite`.** `pnpm dlx` failed with a persistent `ENOENT` on the pnpm dlx cache in this environment (survived a cache clear). Every file `create-vite` would have generated was instead copied from the skill's own templates (`templates/web/*`, `templates/configs/*`) — the templates-first path the skill already prefers, so no functional gap versus the scripted path.
- **`Shell.tsx`'s Home nav item points at `/home`, not `/`,** and `App.tsx`'s index/catch-all routes redirect to `/home`, not the alphabetically-first generated route (`/contacts`). `scripts/generate-pages.ts` has no way to know which page is meant to be the app's landing screen — it defaults to the first route it encounters — so this needed a manual fix to match `docs/00-unified-ia-navigation.md`'s actual IA (Home is the landing screen). Caught by the Playwright login test failing on `/contacts` instead of `/home`.
- **Landing Pages/Forms/Ad Units are not yet in the top-level or Shell nav** — `docs/00-unified-ia-navigation.md` places Landing Pages/Creatives under `Campaigns → Campaign detail`, Ad Units alongside them, and Forms with no dedicated surface at all, but `Shell.tsx`'s nav items are just the three top-level surfaces (Home/Campaigns/Messages); the routes exist (`/landing-pages`, `/forms`, `/ad-units` from `pages:generate`) but nothing links to them yet. Real sub-navigation (a Campaign-detail tab set) is Phase 4 feature-page work, not a Phase 3 shell concern.
- **IA revised 2026-08-27: nav dropped from 5 top-level items to 3 (`Home · Campaigns · Messages`), reversing the earlier `Advertising` label back to `Campaigns` and demoting Contacts and Results/Reports out of the top nav entirely.** Contacts now lives under `Messages → Contacts`; there is no blended "Results"/"Reports" page — each surface gets its own `Performance` section instead (`Campaigns → Performance`, `Messages → Performance`), with Home staying the one operational landing screen. Explicit user preference, not a technical constraint — full rationale in `docs/00-unified-ia-navigation.md`'s "Revision (2026-08-27)" section. `Shell.tsx` was updated to match; the 76 already-generated routes (`/contacts`, `/campaigns`, etc. from `pages:generate`) did not change — only which ones the Shell links to directly.

## Live-DB Testing Process Note

This is a **shared machine running other people's projects** — mid-session, `pnpm --filter web test:e2e` reused a stray dev server on port 3001 (`reuseExistingServer: true`) that turned out to belong to an unrelated project (`codeschooled`), not LOOPIE, causing two confusing false-failure test results before this was diagnosed. Separately, several of this session's own broad `pkill -f "<pattern>"` cleanup calls silently matched nothing (the pattern didn't account for `tsx`'s actual multi-segment invoked command line), leaving stray LOOPIE dev-server processes running for the rest of the session undetected. Neither caused any actual damage here (verified: `codeschooled`'s processes were never touched; the one non-LOOPIE, non-`codeschooled` process a broad `pkill -f "vite"` could plausibly have matched was independently confirmed still running afterward) — but both are exactly the kind of shared-machine hazard worth remembering: **check `ss -ltnp` / `ps aux` for what's *actually* on a port before assuming a service check failure is your own code's fault, and kill dev-server processes by exact PID once identified, not by broad pattern.**

## Last Session Summary

Two large pieces of work landed back-to-back. First, extended the architecture per an explicit brief to add the missing acquisition layer: Landing Pages, Forms, and first-party ad serving, completing the chain `Campaign → Creative → Deployment/AdUnit → LandingPage → Form → Submission → Contact → Lead → Message/Automation → Sale`, following the brief's 10-step order (schema → OpenAPI/SDK → new entities → submission integration → performance aggregation → `apps/ad-server` scaffold → tests). Key decisions honored: AdUnit stays fully separate from Deployment; `apps/ad-server` is a standalone process/Railway service, not a route group; identity resolution is one shared function called from both the pre-existing attribution flow and the new landing-page flow — no second Contact/Lead model anywhere. Caught and fixed a real latent bug in the original Phase 2 `setup.ts` (deleted `automation` before `message` despite a live FK reference).

Then, live-DB verification was requested and run for real against a pre-provisioned MariaDB instance: `db:push` (30-model schema, clean), `db:seed`, both hand-written integration suites (100% passing), and a full manual curl walkthrough of the acquisition chain. This surfaced — and fixed — a real bug no amount of code review had caught: the click redirect never carried the session id forward, so a real visitor's landing-page view/submission could never automatically link back to their click. It only "worked" in earlier testing because the session id was being pulled directly out of the database by hand. Also fixed: parallel Vitest file execution racing against a shared-DB global cleanup, which had been masquerading as spurious auth failures.

Then, in the same session, Phase 3 (frontend shell) was built and verified: full Vite/Tailwind/shadcn-pattern scaffold, all UI primitives, `Shell.tsx` with the real Home · Contacts · Messages · Advertising · Results nav, dark mode, 76 generated page stubs, and — pulled forward from Phase 4 because Phase 3's own gate required a working Home page — SDK domain hooks for every single resource in the spec. Fixed two real bugs in the shared `scripts/generate-pages.ts` generator itself (not just this project's stubs): parameterless singleton GETs like `/home` were misclassified as list pages and crashed calling `.map()` on a plain object; and the index/catch-all routes defaulted to the alphabetically-first generated route (`/contacts`) instead of the actual intended landing screen. Verified for real with Playwright against a live browser, live server, live database — 3/3 passing (login→Home→logout, register, unauthenticated redirect) — not just a typecheck pass, after first misdiagnosing a `codeschooled` port collision as a LOOPIE bug (see "Live-DB Testing Process Note" above).

After that, the top-level IA was revised again per explicit user direction: `Advertising` reverts to `Campaigns` as the literal label, and Contacts/Results drop out of the top nav (Contacts moves under `Messages → Contacts`; Results/Reports is dropped as a nav concept in favor of a `Performance` section within each of Campaigns and Messages). `docs/00-unified-ia-navigation.md` and `Shell.tsx` were updated to match; see the "IA revised 2026-08-27" deviation above for the full before/after and rationale.

Next: Phase 4 — feature pages. Domain hooks (step 19) are already done. What's left: replace the 76 stub pages' `JSON.stringify` placeholders with real component renders, fix the ~25 Create/Update stub pages' naive-string Zod typing (see "Known Gap" above), build the Campaign-detail sub-nav (Landing Pages/Creatives/Platforms/Ad Units/Budget/Performance/Leads) and Messages sub-nav (Contacts/Audiences/Templates/Automations/Performance) per the revised IA doc, and decide/implement the still-open `useDeployment` gap (no standalone `GET /deployments/{id}` exists — see the comment in `useCampaigns.ts`).
