# Project State — LOOPIE

## MVP

**What it does:** A business manages owned communication (email/text/social messages), paid advertising campaigns, LOOPIE-hosted landing pages, first-party ad serving, and forms from one platform, with every lead and sale — regardless of which side generated it — landing in one shared CRM pipeline.

**Users:**

- Business users via `BusinessMembership` (OWNER / MEMBER). The initial business creator is the **Founder** (`isFounder`) — shown as Founder on the team roster and cannot be suspended or removed by others. One user can belong to multiple companies and switch active company from Profile. No MNC-staff-on-behalf-of-client portal; nested departments / custom permission matrices remain parking-lot.

**In V1:**

- Contacts & Audiences — import/create contacts, channel eligibility/consent, segment into audiences (predefined + custom filters)
- Messages — compose/send email & text to an audience, save/reuse templates, automatic follow-up (max 2 steps, source-agnostic `LEAD_CREATED` trigger) that **actually runs** — see "Automation Execution" below
- Advertising — create a campaign (creative + platform + budget + dates), Deployment record per platform, tracked attribution redirect → landing page or lead capture on form submit
- **Landing Pages** — template-driven (not freeform) authoring: `LandingPageTemplate → LandingPage (draft) → PublishedPageVersion (immutable)`. LOOPIE-hosted at `/p/{slug}`, downloadable HTML export, draft/published lifecycle.
- **Forms** — first-class reusable `Form`/`FormField`, referenced by LandingPage. Submission is the canonical identity transition: anonymous session → `FormSubmission` → resolve/create Contact → create Lead, preserving Deployment/AdUnit/UTM attribution.
- **First-party advertising (AdUnit)** — a genuinely separate delivery mechanism from `Deployment` (external platforms). Served by `apps/ad-server`, a standalone deployable service sharing the same database. Feeds the same Lead/Sale spine via `sourceType = AD_UNIT`.
- Leads & Sales — one pipeline (NEW → CONTACTED → QUALIFIED → QUOTED → WON/LOST), Sale recording, attributed back to `sourceType`/`sourceMessageId`/`sourceDeploymentId`/`sourceAdUnitId` regardless of origin
- Results — Home (operational: what needs attention today) plus per-surface Performance (Campaigns → Performance, Messages → Performance); no standalone "Results"/"Reports" nav surface — see the 2026-08-27 IA revision below
- **Money & Ledger** — double-entry ledger for client ad funding, campaign budget authorization, platform spend, LOOPIE fees, credits/refunds, commissions, and payouts. Balances are derived from immutable entries.
- **Affiliate Attribution + Payouts** — a business's own referral partners: referral link/code → tracked session → Contact/Lead → Sale → Commission → Payout, backend-complete (no frontend pages yet) — see "Affiliate Attribution + Payouts" below.
- **Calendar** — goal-driven business coaching and execution center, not project management: `GoalIdeaTemplate` (content) → `lib/coachRules.ts` (which are relevant now, deterministic against real product state) → user schedules into `ScheduledGoal` → Today/This Week → Done. Every idea/goal can carry a `NAVIGATE` action straight to the exact page needed. Coaches through one fixed progression (`CoachStage`: Foundation → Attract → Capture → Convert → Grow). First-class nav tab. See "Calendar" below.
- **Teams / multi-company** — `BusinessMembership` + `BusinessInvitation` + `Session.activeBusinessId`. Profile hosts company switching + team roster; `/team` edits invites/roles/suspension; `/team/members/:userId` shows attributable metrics. Founder is the business creator. See `docs/strategy/loopie-teams-multi-company-mvp-proposal.md`.

**Parking lot (V2+):**

- Live Meta/Google/TikTok API connectors — V1 stores real `Deployment` records but platform status/spend/metrics are entered manually via `PATCH /deployments/{id}`, not synced live.
- Social post publishing to live platforms (V1 = compose/draft only).
- Branching/visual automation builder, >2-step sequences.
- Agency/multi-account/white-label management.
- Billing/subscriptions, deep role/permission systems.
- AI-generated content.
- Drag-and-drop landing-page builder (V1 is intentionally template-constrained — see below).
- Custom-domain DNS/cert provisioning for landing pages (the `customDomain` field exists; nothing automates it yet).
- A "Formerly" external landing-page template catalog/import flow (the data model — `LandingPageTemplate.businessId` nullable, `formatVersion` — is shaped for this, but no import mechanism exists).
- `DATE_REACHED` and `CONTACT_REPLIES` automation triggers — see "Automation Execution" below (**automation execution itself shipped 2026-08-27** — this used to be the whole parking-lot line; it's now just these two triggers).
- Affiliate frontend (portal/payee directory) — the backend chain shipped 2026-08-27 (Affiliate CRUD, referral tracking, auto-Commission on Sale); `pnpm pages:generate` will stub the new `/affiliates/*` routes when wanted.
- Live payment processor — `Payment` records client funding with idempotency/external refs, but no Stripe/processor plugin is installed; funding is recorded, not charged.
- Sales workflow engine (Calendar's `WORKFLOW` source is reserved for it — predefined multi-step sequences that auto-create `ScheduledGoal` rows on lead events). Also deferred: `ENTITY_STATE`/`COUNT` auto-tracking beyond the two wired metrics (page-published, qualified-lead-followups), AI-generated ideas, `OPEN_MODAL`/`START_FLOW` action types (only `NAVIGATE` is implemented), a business-configurable Playbook model (V1 has exactly one, so its order lives in the `CoachStage` enum, not editable rows), a Day calendar view (Month and Year both ship, no Week — see revision 4; List stays the primary/default view overall), `ENTITY_STATE`/`COUNT` auto-tracking for the two newest rules (Messaging/Sales-activity are MANUAL-completion only), and any frontend for reordering/snoozing beyond Complete/Dismiss/Reschedule.

**Stack:** factory defaults (Vite + React, Fastify, Prisma + MySQL) — no overrides. Two Fastify services now: `apps/server` (contract-first, everything account/business-facing) and `apps/ad-server` (plain routes, public first-party ad serving only).

**Plugins:** file-upload (Asset library), transactional email + SMS send (Messages channel delivery) — not yet installed; to confirm against `references/plugin-catalog.md` before Phase 4/5. Ad-platform OAuth connectors deferred to V2. Railway deploy config now exists for both services (`Dockerfile` + `railway.json` each) — see the `railway` plugin and "Production Ops" below for `apps/server`'s, added 2026-08-27.

**Source docs:** `docs/00-unified-data-model.md` (canonical data model), `docs/00-unified-ia-navigation.md` (canonical IA/nav — **Pages is a first-class top-level surface**: `Home · Campaigns · Messages · Ads · Pages · Media`; landing pages are a publishing platform with N ad spaces, not only a campaign destination), plus the Messaging POC and Campaigns doc packs for UX/behavior detail per surface.

## Phase Completed

Phase 1 — Contract (done). Phase 2 — Server (done). Acquisition-chain extension — landing pages, forms, first-party ad serving (done, **live-DB verified**). Phase 3 — Frontend shell (done, **live-DB + real-browser verified** — see below). Phase 4 (feature pages) has started: the generator's Zod-typing gap is fixed and the Campaign → Landing Page acquisition slice is hand-built and **live-DB + real-browser verified** (below). The other ~70 generated pages are still stubs.

## Modules Built

- [x] Monorepo scaffold (pnpm workspaces, root scripts, CI, SDK/DB/api-spec packages)
- [x] Prisma schema — 41 models. Core spine plus landing pages/forms/ad units, plus the 2026-08-26 Money & Ledger set: `FinancialAccount`, `LedgerTransaction`, `LedgerEntry`, `Payment`, `Refund`, `BudgetAuthorization`, `AdSpend`, `Commission`, `Payout`, `PayoutItem`, `Reconciliation`. Compiles (`prisma generate` succeeds) and **pushed clean to a live database**.
- [x] OpenAPI spec — 109 operations. Tags: auth, contacts, audiences, assets, templates, creatives, messages, automations, campaigns, `landing-pages`, `forms`, `ad-units`, attribution, leads, sales, dashboard, **`finance`** (funding, credits, refunds, ledger reverse, ad spend/settle, fees, commissions, payouts, reconciliations) plus campaign budget-authorization/funding and campaign leads.
- [x] SDK — client + generated types + hooks for **every** domain, including `useFinance` / `useFinanceOps`. `packages/sdk` typechecks clean.
- [x] `apps/server` — every operationId has a handler+service. **`FinanceService` is the only writer of ledger rows**; `CampaignService.authorizeBudget` / `recordAdSpend` / `funding` delegate into it. All money-moving methods use Prisma `$transaction`.
- [x] Tests — stubs for all operations, plus real integration suites: `acquisitionPath.test.ts` (2), `adServe.test.ts` (4), and **`financeLedger.test.ts` (6)** covering the $1,000 → $800 auth → $125.37 spend → fee → $25 credit → $40 commission/payout sequence, derived balances, idempotency, tenant isolation, unbalanced rejection, and rollback. **Pass 100% against a live database.**
- [x] `apps/ad-server` — standalone Fastify service (not contract-first — see its README). Routes: `/health`, `/serve/:adUnitId`, `/embed/:adUnitId`, `/impression/:adUnitId`, `/click/:adUnitId`. Own `package.json`, `tsconfig.json`, `Dockerfile`, `railway.json`.
- [x] `apps/web` — Vite + React + Tailwind scaffold (built by hand, not `pnpm create vite` — see Deviations), all UI primitives from `references/frontend-design.md`, `Shell.tsx` with the real nav (**Home · Campaigns · Messages · Ads · Pages · Media**), dark mode wired, 76 page stubs + `App.tsx` generated from the spec, Playwright smoke test. **3/3 Playwright tests pass in a real headless browser against the live server + live DB**: login → Home → logout; register; unauthenticated redirect.
- [x] **Campaign → Landing Page acquisition slice** (2026-08-27) — the first real Phase 4 feature work, hand-built (not generated stubs): `CreateLandingPage.tsx` (template auto-select, name→slug derivation), `LandingPage.tsx` (the editor — content/theme editing driven by the template's own `schema.sections`/`themeTokens` vocabulary, mirroring `renderLandingPage.ts`'s section-type switch so the editor only ever offers fields the real renderer understands; live preview via an iframe fed by `GET /landing-pages/{id}/export`'s real server-rendered HTML — reflects unsaved-then-saved draft state, not a client-side reimplementation of the renderer; form attach/create-inline; Publish; "Use as Campaign Destination" once published), `CampaignPage.tsx`/`CampaignsPage.tsx` (real detail/list, destination resolved by matching `hostedUrl` against `destinationUrl` — landing pages have no `campaignId` FK, see the IA doc's reusable-library resolution), `CreateFormPage.tsx` + `components/forms/FormFieldsEditor.tsx` (hand-built repeatable field editor — `Form.fields` is a required nested-object array with no generic Zod/form-generator equivalent). **Verified live**: a real Playwright spec (`e2e/landing-page-slice.spec.ts`) drives a real browser through the full chain — create campaign → create landing page → edit content → edit theme → attach the seeded form → publish → set as campaign destination → confirm the campaign detail page resolves back to it → confirm the hosted `/p/{slug}` page actually serves the edited content — against the live server + live DB. Passes, alongside the pre-existing `smoke.spec.ts` (still 3/3).
- [x] **Campaign → Budget ledger slice** (2026-08-26) — Campaign detail now has a Budget screen that funds the client wallet, authorizes campaign budget, and records platform spend through `FinanceService`. Planning `Campaign.budget` is labeled as operational; authorized/reserved/reported/settled/available are derived from the ledger. Wallet deposit and campaign authorization are separate cards with distinct labels and confirmation copy. Playwright `e2e/campaign-budget.spec.ts` passes against the live server + live DB.
- [x] **Campaign → Creatives + Ad Units** (2026-08-26) — Campaign detail sub-nav is Overview → Budget → Creatives → Ad Units. Creatives shows what's attached to this campaign (create-and-attach, attach from library, link out to the reusable library). Ad Units are campaign-scoped: create from an attached creative, list serve URL, activate a draft. Playwright `e2e/campaign-running.spec.ts` covers the chain.
- [x] **Campaign → Leads** (2026-08-26) — campaign-scoped outcome list (contact, acquired date, source creative/ad unit/platform, stage, attributed value, last interaction, follow-up). Click opens Contact detail/timeline, not a campaign-specific lead editor. `GET /campaigns/{id}/leads` returns `CampaignLead` rows. Playwright `e2e/campaign-leads.spec.ts` plus `campaignLeads.test.ts`.
- [x] **Automation Execution Engine** (2026-08-27) — full detail in "Automation Execution" below. `AutomationRun` (new model) + `AutomationExecutorService` + `automationScheduling.ts`; 4 of 6 triggers wired for real (`LEAD_CREATED`/`MESSAGE_SENT`/`LEAD_STATUS_CHANGED`/`SALE_RECORDED`), all 6 conditions and 6 actions implemented. `DashboardService.home()`'s two placeholders (`followUpsDue`, `automationErrors`) are now real queries. `automationExecution.test.ts` (5 tests) passes 100% — against a new dedicated `loopie_test` database, not the shared dev DB (see "Shared Database Policy" below).
- [x] **Affiliate Attribution + Payouts** (2026-08-27) — full detail in "Affiliate Attribution + Payouts" below. `Affiliate` + `AffiliateReferralClick` (new models) + `Lead.referringAffiliateId` (new field, deliberately not part of `sourceType`); `AffiliateService` CRUD, `AttributionService.trackAffiliateClick`, one hook in `identityResolution.ts`'s `resolveContactAndLead`, one hook in `SaleService.create()`. Zero changes to `FinanceService` or any finance/security model — `createCommission`/`markCommissionPayable`/`createPayout` were already fully built and already tested. `affiliateAttribution.test.ts` (2 tests) passes 100% against `loopie_test`, alongside `financeLedger.test.ts`/`automationExecution.test.ts`/`acquisitionPath.test.ts` in the same run.
- [x] **Affiliate Policy** (2026-08-27) — full detail in "Affiliate Policy" below. Commission rule type (percentage/fixed, capped), eligibility window (measured from the actual click), reversal (new `POST /sales/{saleId}/reverse` + the pass's one addition to the finance module, `FinanceService.reverseCommission`), payout threshold + cadence (new `AffiliatePayoutService`, a second poller alongside the automation executor), first-touch attribution documented as a decision. `affiliatePolicy.test.ts` (4 tests) passes against `loopie_test` alongside all four other hand-written suites (25 tests total, zero interference).
- [x] **Transactional Integrity Pass** (2026-08-27) — full detail in "Transactional Integrity" below. `MessageService.send()` is now one `$transaction` (was three sequential writes); a real Sale↔Lead contact-mismatch bug fixed in `SaleService.create()`; `LeadService.update()`, `CampaignService.update()`/`pause()`/`resume()`/`end()`, `AudienceService.create()`/`delete()`, and `AutomationExecutorService`'s `SEND_EMAIL`/`SEND_TEXT` action all had their multi-write paths audited and wrapped. `transactionalIntegrity.test.ts` (3 tests, including a real mid-transaction-crash rollback proof via a Prisma `$use` middleware, not a mock) passes against `loopie_test` alongside the other five hand-written suites.
- [x] **Campaign Inventory Reconciliation** (2026-08-27) — full detail in "Campaign Inventory Reconciliation" below. `lib/campaignInventory.ts`'s `reconcileCampaignInventory()` replaces the old create-only `createCampaignInventory()`: updating a campaign's `creativeIds`/`platforms` now deterministically retires (status `ENDED`, never deleted) Deployment/AdUnit combos that dropped out, and creates or revives combos that are newly desired. `UpdateCampaignInput` gained `platforms` (previously immutable after creation, which made this whole class of drift unreachable via the API in the first place). `campaignInventoryReconciliation.test.ts` (3 tests) passes against `loopie_test` alongside the other six hand-written suites.
- [x] **Landing-Page Capture Immutability** (2026-08-27) — full detail in "Landing-Page Capture Immutability" below. `PublishedPageVersion` gained `formSnapshot` (new `lib/formSnapshot.ts`): a Form's fields/submitLabel/successMessage are frozen at publish time, so editing the live Form afterward no longer changes what an already-published page renders or validates submissions against. Also fixed a related, previously-undetected bug: `LandingPageService.submit()` was validating against the page's _live draft_ `formId`, not the pinned `publishedVersion.formId` — a page published against Form A whose draft was later reassigned to Form B would validate submissions against B's live fields while still rendering A's frozen content. `landingPageCaptureImmutability.test.ts` (2 tests) passes against `loopie_test` alongside the other seven hand-written suites.
- [x] **Deployment Contract Gap — Closed** (2026-08-27) — full detail in "Deployment Contract Gap" below. New `GET /deployments/{deploymentId}` operation, `DeploymentService.get()`, `getDeployment` handler; SDK regenerated and `useDeployment()` is now a real fetch instead of the hand-written always-`undefined` stub. `UpdateDeploymentPage.tsx`'s `(data as any)?.data` cast (the visible symptom of the gap) is now a plain, correctly-typed `data?.data`. `deploymentContract.test.ts` (3 tests) passes against `loopie_test` alongside the other eight hand-written suites.
- [x] **Production Ops** (2026-08-27) — full detail in "Production Ops" below. Public rate limiting is now DB-backed (`packages/db`'s new `consumeRateLimit`/`RateLimitBucket`) instead of a process-local `Map`, in both `apps/server` and `apps/ad-server`. `apps/server` gained its own `Dockerfile`/`railway.json` (mirroring `apps/ad-server`'s). Found and fixed a real, previously-unexercised bug along the way: both services' `"start": "node dist/index.js"` script would crash immediately in production (`@project/db` has no build step of its own — its `package.json#main` points straight at `.ts` source, which plain `node` can't load), confirmed by actually running it; both now use `tsx` in production, matching the Dockerfiles and `pnpm dev`. Added a structural `assertTestDatabaseUrl()` guard (not just a config default) so the test suites can never accidentally wipe a non-test database. `rateLimit.test.ts` (6 tests in `apps/server`, 1 in `apps/ad-server`) passes against `loopie_test`.
- [x] **Calendar** (2026-09-02) — full detail in "Calendar" below. New models `GoalIdeaTemplate`/`GoalIdeaState`/`ScheduledGoal`/`GoalEvent`; `CalendarService` (board read, create/schedule/dismiss ideas, complete/reschedule goals, live progress computation) + `lib/coachRules.ts` (the eligibility/prerequisite/action-resolution rules layer, kept separate from orchestration per the product's own Ideas/Rules/Playbook vocabulary) + `CalendarReminderService` (a fifth `worker.ts` poller). First-class `/calendar` nav tab, minimal four-list UI (Today/This Week/Recently Completed/Ideas), no dashboard/kanban chrome. Scheduling an idea is one click (defaults to "this week" with its own default estimate, no popover); the full When/Pick-date/Estimate control and a fuller "why this matters" view expand inline on a title tap (no modal — a bare `useState` toggle per row, not a dialog). **List/Calendar split (same day, revision 2)**: an idea is "just a possibility" and deliberately carries no action destination (`toGoalIdeaDTO` omits it entirely — Schedule/Dismiss only); a _scheduled_ `ScheduledGoal` does, shown as an always-visible link under the row (committed work earns it). Completing a goal keeps it visible under Recently Completed (last 14 days, capped 8) instead of vanishing. A `[list]/[calendar]` switch renders the exact same rows a second way. **Month grid pass (revision 3)**: Calendar view defaults to a real Month grid (`MonthView`) — every goal on its actual `scheduledFor` date, prev/next/Today navigation, click a day to open `DayDetailPanel` (full `GoalRow` treatment + that day's own quick-add). Both Month and (at the time) Week read a new `GET /calendar/goals?from&to` (`CalendarService.listGoalsInRange`, `useCalendarGoalsInRange`) — an arbitrary, navigable date range, unlike the board's fixed "relative to now" buckets, so the calendar can actually show last/next month. No idea-chip rail (tried and explicitly rejected): "ideas are things users add at the day level" means typing directly into a day's quick-add (`QuickAddToDay`, create+schedule in one action) — the day picked is already the scheduling decision. **Refinement pass (revision 4, same day)**: Week mode replaced outright with **Year** (`YearView`, WeekView/WeekDayItem deleted) — 12 month tiles, counts + a capped dot row only, no task rendering, click a tile to jump into Month there, per "very lightweight overview only." Calendar view now also carries its own **Upcoming/Recently Completed/Ideas** sections below the grid (reusing the exact board data List view shows — the grid answers "when," these lists answer "what"), with the Ideas section's Schedule button day-aware (`ScheduleTarget`: the open day if one's selected, else today) — this is what "ideas added at the day level" resolves to outside the day panel too. `GoalRow`'s action button (`ActionButton`, `Button`-styled) moved out of the always-visible row into the expanded panel, alongside Mark done/Reschedule, matching those controls instead of sitting apart as a plain text link. The top persistent input is now **`QuickAddTask`** ("Add a task…") — it create-and-schedules immediately for Today, same composition as `QuickAddToDay`; the old create-only behavior moved to **`AddIdeaInline`**, a click-to-reveal "+ Add idea" button next to the now-persistent Ideas section header (shown even when empty, in both List and Calendar view) rather than the page header. **Coach coverage extended** to the two remaining core currencies from the original product spec: `NO_MESSAGES_SENT_EVER` ("Send an update to your audience" → `/messages/new`) and `WON_LEAD_WITHOUT_SALE` ("Record a recent sale" → `/sales/new`) — Pages/Ads/CRM/Messaging/River/Sales-activity are now all represented by at least one real rule in `lib/coachRules.ts`. CRM integration reuses the existing `Lead.nextActionAt`/`ContactLeadCard` "Next action" surface rather than adding a parallel one — `LeadService.update()` mirrors it into a `ScheduledGoal` (`CRM_NEXT_ACTION` source, `externalKey` upsert), and `ContactService.logActivity()` auto-completes it, closing the CRM-state → scheduled-work → human-action → CRM-activity loop the product spec calls for. **Command-center pass (same day)**: every idea/goal can carry a real in-app destination (`actionType`/`actionTarget`/`actionLabel` — only `NAVIGATE` implemented) rendered as a one-click "Open X" button — a couple (the specific draft page/ad) are resolved live in `resolveAction`, not stored as a fixed path. **Coach progression**: `CoachStage` enum (Foundation → Attract → Capture → Convert → Grow, one fixed universal playbook, not a configurable model) on every template; `GoalIdeaTemplate.requiresTemplateIds` is the deterministic unlock gate ("completing work unlocks the next useful suggestion") — satisfied either by a DONE `ScheduledGoal` or by a dynamic prerequisite's live condition already being met (so a business that did the real work outside Calendar isn't locked out); the board's `currentStage` is a computed narrative label, not a gate. **Reserve-library pass (revision 5)**: the catalog grew from ~15 to 41 templates spanning real solo-business-journey steps (define your offer, set pricing, write your bio, case studies, lead magnets, referral incentives, quotes, upsells, repeat business, monthly review, …), every `detail` trimmed to exactly one plain sentence. Ideas pool cap raised 6 → 18 (the product's "10-30 visible, much larger reserve" band) via `CalendarService.MAX_IDEAS`. New `GoalIdeaTemplate.repeatable`/`cooldownDays` fields (only consulted for a _static_ template — a dynamic one's own live condition is already its natural recurrence rule, see `cooldownBlocks`): false/null (the default) means one-time, gone for good once a DONE goal exists; true+N means it can come back N days after its most recent completion (`lastCompletionByTemplate`, one batched query). Final selection is no longer a flat sort+cap — `CalendarService.selectDiverse` round-robins across `subjectType` _within_ each stage tier before moving to the next stage, so the visible slice stays spread across Pages/Ads/CRM/River instead of one category crowding the rest out; stage order (Foundation before Grow) still isn't negotiable. Two more dynamic rules — `NEW_LEAD_UNTOUCHED` ("contact your newest lead today") and `HAS_ANY_ADVERTISEMENT` ("try a new ad audience") — plus three new progress-only metrics giving Sales/River/CRM their own COUNT goals (`SALES_AMOUNT_SINCE` — a real `Decimal` sum aggregate, `RIVER_POSTS_SINCE`, `CONTACTS_ADDED_SINCE`). Refill is still the same mechanism as always (eligibility is recomputed live on every board read, so scheduling/dismissing/completing/state-changing all naturally surface the next-best eligible template) — this pass is what makes that mechanism scale to a library instead of a short fixed list. `calendar.test.ts` (12 tests, including cooldown-elapses-then-reappears and reserve-size assertions) passes against `loopie_test`; `e2e/calendar.spec.ts` passes against the live server + live DB in a real browser. `packages/db/prisma/seed/calendar.ts` seeds business-specific ideas/scheduled CRM work for both demo tenants. **"Big real goals" pass (revision 6, same day)**: `MAX_IDEAS` dropped 18 → 6 — the product call once the reserve library was deep enough that a wide visible cap started surfacing granular chores alongside the genuinely big must-dos; six is a handful of real next moves, not a checklist, and is backed by the same 39-template reserve + repeatable/cooldown refill so completing everything once doesn't run the well dry. Two weak templates removed outright (`system-idea-hours-location`, and `system-idea-add-contact-form` — "Review your homepage form," the exact example flagged as a chore, not a goal), and several titles/priorities tightened to read as outcomes: `system-idea-publish-homepage` renamed "Launch your homepage" and bumped to the second-highest priority weight in the catalog (just under the business profile itself) so it reliably wins a slot for a new business; "Create a free guide or checklist to offer" → "Create a lead magnet"; "Add a 'book a call' or 'get a quote' button" → "Add a booking or quote button." Net effect: for a brand-new business, all 6 visible slots are now legitimately Foundation-tier (business profile, launch homepage, connect CRM, define your offer, find your first audience, set your pricing) — `CalendarService.selectDiverse`'s strict stage ordering means later-stage ideas simply don't compete until Foundation's own count drops under the cap. `calendar.test.ts`'s presence assertions for lower-priority/later-stage templates were generalized to a `surfaceTemplate()` test helper (dismiss whatever currently occupies a slot until the template under test surfaces) instead of assuming a fixed board composition — the same technique now protects those tests against future priority/cap tuning. 12/12 pass; full server suite is otherwise green (the 3 pre-existing `businessProfileV2`/`riverFeedV2` failures are unrelated, concurrent in-progress work). **Content pass + a real orphan-deletion bug fix (revision 7, same day)**: 7 new static templates covering gaps the user named directly — `system-idea-upload-photos` (Media), `system-idea-facebook-profile` + `system-idea-link-facebook` (prerequisite-chained), `system-idea-pitch-email`, `system-idea-release-party`, and a worked instance of CLAUDE.md's own "publish page → promote it" example: `system-idea-publish-lead-magnet-page` (requires the existing `system-idea-lead-magnet`) → `system-idea-promote-lead-magnet-page` (requires that page be published) — catalog now 48 templates. Along the way, found and fixed a real latent crash: `ensureSystemGoalIdeaTemplates`'s orphan-deletion (added in revision 6 to retire weak content) deleted `GoalIdeaTemplate` rows by id without accounting for `ScheduledGoal.sourceTemplateId` being a real FK (`ScheduledGoal.template`) — any business with a past (even completed) goal against a retired id would 500 on every future board read. Fixed in both `ensureSystemGoalIdeaTemplates` and the seed script's duplicate: null out referencing `sourceTemplateId`s (the goal's own title/detail were already copied at creation time, so nothing visible changes) before deleting, scoped strictly to `isSystem` orphans so a business's own custom "+ Add idea" templates (also absent from the seed list) are never swept up. `calendar.test.ts` gained a regression test creating a retired template + a DONE goal referencing it and asserting the board read survives and nulls the reference; 13/13 pass. **Day-grouping pass (revision 8, same day)**: the product call after revision 7 was to stop adding template content and instead make the idea→scheduled→completed lifecycle itself feel finished — This Week and Recently Completed were flat scheduledFor/completedAt-sorted lists with no visual structure once more than a couple of items landed in them. New `apps/web`-only helpers in `CalendarPage.tsx`: `dayGroupLabel` (Today/Tomorrow/a weekday name within 6 days/short date beyond that, looking forward; Today/Yesterday/"Earlier this week"/"Earlier" looking back) and `groupConsecutive` (clusters an already-sorted list by that label without re-sorting) feed a new `GroupedGoalList` component, used for This Week + Recently Completed in List view and the equivalent Upcoming + Recently Completed lists in Calendar view — the same clustering logic in both, so the two projections stay visually consistent per the "no separate calendar-specific UX" rule. `Section` gained a `bare` prop so `GroupedGoalList` can supply its own per-cluster hairline instead of double-bordering inside the section's existing wrapper. No backend changes. Caught a stale reference in `e2e/calendar.spec.ts` along the way: the CAP=6 change from revision 6 (same day, earlier) had crowded `system-idea-ask-for-reviews` out of the demo account's visible pool, so the browser test's hard-coded reference to it had gone stale exactly like the backend tests fixed in that revision — never caught because no one had re-run the Playwright spec since. Swapped to `system-idea-first-audience` (a Foundation-tier idea that reliably survives the cap) and verified via a real headless-browser run against an isolated scratch server + Vite instance. Screenshot-verified the actual grouped rendering (Month grid keeps completed items visible, struck-through, on their real date; List/Calendar both show Tomorrow/Saturday/Sunday and Today/Earlier this week sub-headers). Per this same product direction, template-catalog growth is paused for now — 48 is enough to validate the system; the next useful investment is lifecycle polish, not more content. **Continuation-boost pass (revision 9, same day) — Calendar/Ideas declared complete.** Manually walking the Facebook and lead-magnet chains end-to-end (idea → schedule → complete → successor appears → schedule → complete, verified after real page reloads) surfaced one real UX gap: a successor's _eligibility_ unlocks immediately, but with `MAX_IDEAS` at 6 and a stage often already full, the newly-unlocked idea doesn't always win a visible slot on ordinary priority alone (`system-idea-promote-lead-magnet-page`, priority 5, stayed crowded out by `system-idea-launch-first-ad`, priority 12, in the same Capture/Advertisement category) — forcing a user to dismiss unrelated ideas just to see the obvious next step. Fixed with a "continuation boost" in `CalendarService.ts`, deliberately stateless: `listIdeas` already computes `lastCompletionByTemplate` (every template's most recent completion) for the cooldown rule, so finding the single most-recently-completed template across all of them is free — no new query, no schema change. Any eligible idea whose `requiresTemplateIds` names that one template is `boostedIds`-marked; a new `applyContinuationBoost` runs after `selectDiverse` and, only for a boosted idea that didn't make the diverse top 6 on its own, evicts the current _lowest-priority non-boosted_ slot to make room (never evicts another boosted idea, never exceeds the cap, never touches `priorityWeight` or `selectDiverse`'s own stage/category logic — a one-read swap, not a reorder). Because it's derived live from `ScheduledGoal.completedAt` rather than a persisted flag, it applies uniformly no matter which path completed the prerequisite (manual "Mark done," `syncTrackedGoals`'s auto-complete, `completeCrmWorkOnActivity`, `completePagePublishGoals`) and decays the instant anything else completes — there is nothing to expire or clean up. `calendar.test.ts` gained a fully deterministic regression test (six synthetic Foundation-stage filler templates, one per category, guaranteed to fill the cap on priority alone, so every assertion is attributable to the boost and not incidental catalog crowding) proving: the successor is absent beforehand, present (as a single-slot swap, not a wholesale reorder) the instant its prerequisite completes with no further dismissals, and gone again once an unrelated completion moves the boost on — confirmed to actually fail without the fix by temporarily reverting it and re-running. 14/14 pass; full server suite otherwise unchanged (413 passing, same 3 pre-existing unrelated failures). Re-verified live in a real browser afterward: completing "Publish a page for your lead magnet" now surfaces "Advertise your lead magnet page" immediately alongside "Launch your first ad" (the very idea that used to crowd it out), at the cost of exactly one lower-priority idea. This closes out Calendar/Ideas as a product area for now — the next work should go to a different surface, not further Calendar features.

**Calendar reacts to real CRM/sales activity (2026-09-03).** A separate, already-committed pass (`8afd8f9`, not this work) simplified `LeadStage` to `NEW/UNDECIDED/INTERESTED/CLOSED/NOT_INTERESTED` and added a small Status + Activity model on the open `Lead` itself: seven boolean flags (`emailed`/`called`/`texted`/`webinar`/`meeting`/`followUp`/`proposalSent`, `lib/leadActivity.ts`'s `LEAD_ACTIVITY_FLAGS`) auto-set from real events (a real send via `MessageService`, a manually logged `Interaction` via `ContactService.logActivity`) and still manually toggleable, redesigning the contact sales panel around Status + Activity + Next. This pass is what makes Calendar's coach actually _read_ that data, closing the "who am I talking to, what happened, what should I do next" loop with two new dynamic ideas and one auto-completion gap: `lib/coachRules.ts` gained `LEADS_FLAGGED_FOR_FOLLOW_UP` (any open lead someone explicitly checked "Follow-up" — the most direct, lowest-inference signal the CRM has) and `INTERESTED_LEAD_NEEDS_PROPOSAL` (an open `INTERESTED` lead with no proposal sent yet — deliberately stage-only, not further gated on "has been contacted," since a lead can reach `INTERESTED` from a form submission with no logged activity at all); both new `goalIdeas.ts` templates (`system-idea-leads-flagged-for-follow-up`, `system-idea-send-proposal-to-interested`) live at Convert stage alongside the existing lead-follow-up ideas. Separately, `completeCrmWorkOnActivity` — the hook that closes a CRM-linked Calendar task when real contact happens — was only ever wired to the manual "log activity" path; `MessageService.send()` now calls it too, so an actual email/text send (not just a manually logged interaction) closes a "Follow up with X" task the same way. `calendar.test.ts` gained three tests: both new dynamic ideas' eligibility (present while the condition holds, gone once cleared) and a real end-to-end message-send test (create a CRM next-action goal, send a real message through `POST /messages` + `/send`, confirm the goal auto-completes) — 17/17 pass; full server suite otherwise unchanged (416 passing, same 3 pre-existing unrelated failures). No schema, OpenAPI, or frontend changes — this was purely `apps/server`'s coach-rules/content layer consuming fields the CRM pass already exposes. Verified live against the demo account, which also surfaced and fixed a real piece of session debris: an earlier verification pass's cleanup had deleted stray `ScheduledGoal` rows but not the underlying custom (`isSystem:false`) `GoalIdeaTemplate` rows they came from, which (being always-eligible at `priorityWeight:100`) were quietly occupying real slots in the demo account's visible pool.

Note: mid-session, a concurrent editing pass elsewhere in the repo restructured `LeadStage` (`NEW`/`CONTACTED`/`ENGAGED`/`QUALIFIED`/`PROPOSAL`/`WON`/`LOST` → `NEW`/`UNDECIDED`/`INTERESTED`/`CLOSED`/`NOT_INTERESTED`) and updated `lib/coachRules.ts`'s stage references to match (`CLOSED` = won, `INTERESTED` = the one "stale qualified" stage) — accepted as-is per that pass's own doc comment on the enum, not something this work reverted.

- [x] **Assistant becomes the consultant** (2026-09-04) — the POC described in `docs/loopie-assistant-playbook-poc/` implemented against real code, reconciling its Learn→Act→Review→Grow model with the two existing systems it conflated: the Assistant panel (a stateless Business→Pages→Ads nudge chain) and Calendar/`CoachStage` (which already did most of the Act-phase eligibility/execution work). Decision: Assistant absorbs the new engine and stays the single entry point; Calendar/`CoachStage`/`coachRules.ts` are untouched and keep governing the generic Ideas feed underneath. New `apps/server/src/business-guidance/` content layer (taxonomy/goals/knowledge/questions/playbooks/phaseEngine/signals — declarative, no Prisma) implements the docs' venture taxonomy (adapted from `04-expandable-taxonomy-poc.ts`) and two POC playbooks, `LOCAL_SERVICE_GET_CUSTOMERS` (matched by the `LOCAL` trait — covers Roofing and every other local-service venture) and `WEB_DEVELOPMENT_GET_CUSTOMERS` (matched by exact venture type) — both reuse existing `GoalIdeaTemplate` rows for 4 of 6 steps each (only `system-idea-define-primary-offer` and `system-idea-contact-prospects` are genuinely new templates, added to `goalIdeas.ts`), per the docs' "don't build a second task universe" rule. New `Business.knowledge Json?` column (durable taxonomy/goal/constraint facts with no existing home; `targetCustomer`/`serviceArea` reuse `targetAudience`/`location` instead of duplicating them) and a new `AssistantGoalCycle` model (`AssistantPhase` LEARN/ACT/REVIEW/GROW, `AssistantReviewOutcome`, one ACTIVE cycle per business) plus a `ScheduledGoal.assistantGoalCycleId` FK and `ASSISTANT_PLAYBOOK` source value so a playbook's scheduled steps are queryable as a batch — migration `20260904000000_assistant_goal_cycle`. New `AssistantGoalCycleService` (Learn question resolution/answering, Act plan preview + real Calendar scheduling via an extended `CalendarService.scheduleIdea` with an additive `overrides` param, Review classification, Grow → next cycle) and `AssistantSignalService` (three pull-based/compute-on-read reactive signals — page traffic with no leads, interested leads needing follow-up, a sale recorded — deduped via the cycle's own `lastSignalKey`; a fourth, ad-clicks-without-leads, deferred per the docs' own "only if reliable" caveat, matching the parking-lot precedent). `AssistantService.getNextAction`'s existing Business→Page→Advertising chain is unchanged; two new priority slots were added around it (an urgent signal above, the active cycle's Learn/Plan/Grow step below) — **intentional behavior change**: once that chain is satisfied, a business with no active goal cycle now sees a "Tell Loopie about your business" prompt instead of the old unconditional Calendar-card fallback; Calendar is reached again once a cycle's plan is actually scheduled. Five new operations (`POST /assistant/goal-cycle/{answer,schedule-plan,review,grow,dismiss-signal}`) and an extended `AssistantActionDTO` (new `GOAL_CYCLE`/`SIGNAL` types, additive so existing SDK consumers are unaffected); new SDK hooks in `useAssistant.ts`. Frontend: `AssistantChoiceStepView`/`AssistantPlanView`/`AssistantGrowView`/`AssistantSignalCard`, rendered directly on the panel's Home view (same "no click-through, just one card" pattern the existing Calendar card already used) rather than behind Flow's click-through screens. 16 unit tests (taxonomy traversal, known-fact skipping, playbook specificity precedence, phase transitions) plus a full HTTP-integration suite (`assistantGoalCycle.test.ts`) walking the entire Roofing path — Learn through a sale-triggered signal auto-advancing Review→Grow→a fresh Act cycle with zero repeated questions — and a shorter Web Development traversal proving the taxonomy/playbook selection is data-driven; full server suite otherwise green (466 passing). Verified live: `e2e/assistant.spec.ts` updated for the new terminal behavior and re-run against a real browser + live server/DB, confirming the taxonomy UI actually renders and advances. Deferred/simplified for POC scope: no "Back" within a Learn question sequence (only the panel's own Assistant-Home escape hatch), Review is computed transiently and lands directly on Grow rather than resting as its own phase, and only `GET_MORE_CUSTOMERS` gets full playbook treatment (per the docs' own scoping).

- [ ] Remaining feature pages — Phase 4. The other ~70 generated pages are still stubs (list/detail pages use `JSON.stringify` placeholders); their Create/Update forms now have correctly-typed fields but still need real component design.
- [ ] Admin panel (not needed — no admin role in V1 scope)
- [ ] Polish (Phase 5)
- [ ] Documentation (Phase 6)
