# Project State — LOOPIE

## MVP

**What it does:** A business manages owned communication (email/text/social messages), paid advertising campaigns, LOOPIE-hosted landing pages, first-party ad serving, and forms from one platform, with every lead and sale — regardless of which side generated it — landing in one shared CRM pipeline.

**Users:**
- Single role in V1: Business user (owner/manager/marketer). No MNC-staff-on-behalf-of-client portal, no agency/multi-account layer.

**In V1:**
- Contacts & Audiences — import/create contacts, channel eligibility/consent, segment into audiences (predefined + custom filters)
- Messages — compose/send email & text to an audience, save/reuse templates, automatic follow-up (max 2 steps, source-agnostic `LEAD_CREATED` trigger) that **actually runs** — see "Automation Execution" below
- Advertising — create a campaign (creative + platform + budget + dates), Deployment record per platform, tracked attribution redirect → landing page or lead capture on form submit
- **Landing Pages** — template-driven (not freeform) authoring: `LandingPageTemplate → LandingPage (draft) → PublishedPageVersion (immutable)`. LOOPIE-hosted at `/p/{slug}`, downloadable HTML export, draft/published lifecycle.
- **Forms** — first-class reusable `Form`/`FormField`, referenced by LandingPage. Submission is the canonical identity transition: anonymous session → `FormSubmission` → resolve/create Contact → create Lead, preserving Deployment/AdUnit/UTM attribution.
- **First-party advertising (AdUnit)** — a genuinely separate delivery mechanism from `Deployment` (external platforms). Served by `apps/ad-server`, a standalone deployable service sharing the same database. Feeds the same Lead/Sale spine via `sourceType = AD_UNIT`.
- Leads & Sales — one pipeline (NEW → CONTACTED → QUALIFIED → QUOTED → WON/LOST), Sale recording, attributed back to `sourceType`/`sourceMessageId`/`sourceDeploymentId`/`sourceAdUnitId` regardless of origin
- Results — Home (operational: what needs attention today) plus per-surface Performance (Campaigns → Performance, Messages → Performance); no standalone "Results"/"Reports" nav surface — see the 2026-08-27 IA revision below
- **Money & Ledger** — double-entry ledger for client ad funding, campaign budget authorization, platform spend, LOOPIE fees, credits/refunds, commissions, and payouts. Balances are derived from immutable entries.
- **Affiliate Attribution + Payouts** — a business's own referral partners: referral link/code → tracked session → Contact/Lead → Sale → Commission → Payout, backend-complete (no frontend pages yet) — see "Affiliate Attribution + Payouts" below.

**Parking lot (V2+):**
- Live Meta/Google/TikTok API connectors — V1 stores real `Deployment` records but platform status/spend/metrics are entered manually via `PATCH /deployments/{id}`, not synced live.
- Social post publishing to live platforms (V1 = compose/draft only).
- Branching/visual automation builder, >2-step sequences.
- Agency/multi-account/white-label management.
- Billing/subscriptions, deep role/permission systems.
- AI-generated content.
- Drag-and-drop landing-page builder (V1 is intentionally template-constrained — see below).
- Custom-domain DNS/cert provisioning for landing pages (the `customDomain` field exists; nothing automates it yet).
- A "Formerly" external landing-page template catalog/import flow (the data model — `LandingPageTemplate.businessId` nullable, `formatVersion` — is shaped for this, but no import mechanism exists).
- `DATE_REACHED` and `CONTACT_REPLIES` automation triggers — see "Automation Execution" below (**automation execution itself shipped 2026-08-27** — this used to be the whole parking-lot line; it's now just these two triggers).
- Affiliate frontend (portal/payee directory) — the backend chain shipped 2026-08-27 (Affiliate CRUD, referral tracking, auto-Commission on Sale); `pnpm pages:generate` will stub the new `/affiliates/*` routes when wanted.
- Live payment processor — `Payment` records client funding with idempotency/external refs, but no Stripe/processor plugin is installed; funding is recorded, not charged.

**Stack:** factory defaults (Vite + React, Fastify, Prisma + MySQL) — no overrides. Two Fastify services now: `apps/server` (contract-first, everything account/business-facing) and `apps/ad-server` (plain routes, public first-party ad serving only).

**Plugins:** file-upload (Asset library), transactional email + SMS send (Messages channel delivery) — not yet installed; to confirm against `references/plugin-catalog.md` before Phase 4/5. Ad-platform OAuth connectors deferred to V2. Railway deploy config exists for `apps/ad-server` only (`apps/ad-server/Dockerfile` + `railway.json`) — `apps/server` doesn't have one yet, see the `railway` plugin.

**Source docs:** `docs/00-unified-data-model.md` (canonical data model), `docs/00-unified-ia-navigation.md` (canonical IA/nav — **revised 2026-08-27**: `Home · Campaigns · Messages`, replacing the earlier `Home · Contacts · Messages · Advertising · Results`; "Campaigns" is the literal top-level label again, Contacts moved under Messages, Results/Reports dropped as a nav concept entirely in favor of per-surface Performance), plus the Messaging POC and Campaigns doc packs for UX/behavior detail per surface.

## Phase Completed

Phase 1 — Contract (done). Phase 2 — Server (done). Acquisition-chain extension — landing pages, forms, first-party ad serving (done, **live-DB verified**). Phase 3 — Frontend shell (done, **live-DB + real-browser verified** — see below). Phase 4 (feature pages) has started: the generator's Zod-typing gap is fixed and the Campaign → Landing Page acquisition slice is hand-built and **live-DB + real-browser verified** (below). The other ~70 generated pages are still stubs.

## Modules Built

- [x] Monorepo scaffold (pnpm workspaces, root scripts, CI, SDK/DB/api-spec packages)
- [x] Prisma schema — 41 models. Core spine plus landing pages/forms/ad units, plus the 2026-08-26 Money & Ledger set: `FinancialAccount`, `LedgerTransaction`, `LedgerEntry`, `Payment`, `Refund`, `BudgetAuthorization`, `AdSpend`, `Commission`, `Payout`, `PayoutItem`, `Reconciliation`. Compiles (`prisma generate` succeeds) and **pushed clean to a live database**.
- [x] OpenAPI spec — 109 operations. Tags: auth, contacts, audiences, assets, templates, creatives, messages, automations, campaigns, `landing-pages`, `forms`, `ad-units`, attribution, leads, sales, dashboard, **`finance`** (funding, credits, refunds, ledger reverse, ad spend/settle, fees, commissions, payouts, reconciliations) plus campaign budget-authorization/funding and campaign leads.
- [x] SDK — client + generated types + hooks for **every** domain, including `useFinance` / `useFinanceOps`. `packages/sdk` typechecks clean.
- [x] `apps/server` — every operationId has a handler+service. **`FinanceService` is the only writer of ledger rows**; `CampaignService.authorizeBudget` / `recordAdSpend` / `funding` delegate into it. All money-moving methods use Prisma `$transaction`.
- [x] Tests — stubs for all operations, plus real integration suites: `acquisitionPath.test.ts` (2), `adServe.test.ts` (4), and **`financeLedger.test.ts` (6)** covering the $1,000 → $800 auth → $125.37 spend → fee → $25 credit → $40 commission/payout sequence, derived balances, idempotency, tenant isolation, unbalanced rejection, and rollback. **Pass 100% against a live database.**
- [x] `apps/ad-server` — standalone Fastify service (not contract-first — see its README). Routes: `/health`, `/serve/:adUnitId`, `/embed/:adUnitId`, `/impression/:adUnitId`, `/click/:adUnitId`. Own `package.json`, `tsconfig.json`, `Dockerfile`, `railway.json`.
- [x] `apps/web` — Vite + React + Tailwind scaffold (built by hand, not `pnpm create vite` — see Deviations), all UI primitives from `references/frontend-design.md`, `Shell.tsx` with the real nav (**Home · Campaigns · Messages**, revised 2026-08-27 — see Deviations), dark mode wired, 76 page stubs + `App.tsx` generated from the spec, Playwright smoke test. **3/3 Playwright tests pass in a real headless browser against the live server + live DB**: login → Home → logout; register; unauthenticated redirect.
- [x] **Campaign → Landing Page acquisition slice** (2026-08-27) — the first real Phase 4 feature work, hand-built (not generated stubs): `CreateLandingPage.tsx` (template auto-select, name→slug derivation), `LandingPage.tsx` (the editor — content/theme editing driven by the template's own `schema.sections`/`themeTokens` vocabulary, mirroring `renderLandingPage.ts`'s section-type switch so the editor only ever offers fields the real renderer understands; live preview via an iframe fed by `GET /landing-pages/{id}/export`'s real server-rendered HTML — reflects unsaved-then-saved draft state, not a client-side reimplementation of the renderer; form attach/create-inline; Publish; "Use as Campaign Destination" once published), `CampaignPage.tsx`/`CampaignsPage.tsx` (real detail/list, destination resolved by matching `hostedUrl` against `destinationUrl` — landing pages have no `campaignId` FK, see the IA doc's reusable-library resolution), `CreateFormPage.tsx` + `components/forms/FormFieldsEditor.tsx` (hand-built repeatable field editor — `Form.fields` is a required nested-object array with no generic Zod/form-generator equivalent). **Verified live**: a real Playwright spec (`e2e/landing-page-slice.spec.ts`) drives a real browser through the full chain — create campaign → create landing page → edit content → edit theme → attach the seeded form → publish → set as campaign destination → confirm the campaign detail page resolves back to it → confirm the hosted `/p/{slug}` page actually serves the edited content — against the live server + live DB. Passes, alongside the pre-existing `smoke.spec.ts` (still 3/3).
- [x] **Campaign → Budget ledger slice** (2026-08-26) — Campaign detail now has a Budget screen that funds the client wallet, authorizes campaign budget, and records platform spend through `FinanceService`. Planning `Campaign.budget` is labeled as operational; authorized/reserved/reported/settled/available are derived from the ledger. Wallet deposit and campaign authorization are separate cards with distinct labels and confirmation copy. Playwright `e2e/campaign-budget.spec.ts` passes against the live server + live DB.
- [x] **Campaign → Creatives + Ad Units** (2026-08-26) — Campaign detail sub-nav is Overview → Budget → Creatives → Ad Units. Creatives shows what's attached to this campaign (create-and-attach, attach from library, link out to the reusable library). Ad Units are campaign-scoped: create from an attached creative, list serve URL, activate a draft. Playwright `e2e/campaign-running.spec.ts` covers the chain.
- [x] **Campaign → Leads** (2026-08-26) — campaign-scoped outcome list (contact, acquired date, source creative/ad unit/platform, stage, attributed value, last interaction, follow-up). Click opens Contact detail/timeline, not a campaign-specific lead editor. `GET /campaigns/{id}/leads` returns `CampaignLead` rows. Playwright `e2e/campaign-leads.spec.ts` plus `campaignLeads.test.ts`.
- [x] **Automation Execution Engine** (2026-08-27) — full detail in "Automation Execution" below. `AutomationRun` (new model) + `AutomationExecutorService` + `automationScheduling.ts`; 4 of 6 triggers wired for real (`LEAD_CREATED`/`MESSAGE_SENT`/`LEAD_STATUS_CHANGED`/`SALE_RECORDED`), all 6 conditions and 6 actions implemented. `DashboardService.home()`'s two placeholders (`followUpsDue`, `automationErrors`) are now real queries. `automationExecution.test.ts` (5 tests) passes 100% — against a new dedicated `loopie_test` database, not the shared dev DB (see "Shared Database Policy" below).
- [x] **Affiliate Attribution + Payouts** (2026-08-27) — full detail in "Affiliate Attribution + Payouts" below. `Affiliate` + `AffiliateReferralClick` (new models) + `Lead.referringAffiliateId` (new field, deliberately not part of `sourceType`); `AffiliateService` CRUD, `AttributionService.trackAffiliateClick`, one hook in `identityResolution.ts`'s `resolveContactAndLead`, one hook in `SaleService.create()`. Zero changes to `FinanceService` or any finance/security model — `createCommission`/`markCommissionPayable`/`createPayout` were already fully built and already tested. `affiliateAttribution.test.ts` (2 tests) passes 100% against `loopie_test`, alongside `financeLedger.test.ts`/`automationExecution.test.ts`/`acquisitionPath.test.ts` in the same run.
- [ ] Remaining feature pages — Phase 4. The other ~70 generated pages are still stubs (list/detail pages use `JSON.stringify` placeholders); their Create/Update forms now have correctly-typed fields but still need real component design.
- [ ] Admin panel (not needed — no admin role in V1 scope)
- [ ] Polish (Phase 5)
- [ ] Documentation (Phase 6)

## Known Gap: Generated Stub Pages Have Naive Field Types — FIXED (2026-08-27)

`scripts/generate-pages.ts`'s Zod-schema inference (`buildZodExpr`) used to only ever produce `z.string()` — it didn't read the OpenAPI property's actual `type`/`enum`/array-ness. Every generated Create/Update page's form data was therefore all-strings, which didn't match the correctly-typed domain hooks (real enums, `number`, `string[]`, `Record<string, unknown>`). This was a pre-existing, systemic generator limitation (confirmed present for the original Contacts/Audiences pages too), not something the hook-writing session introduced — but it was the explicit blocker called out before starting Phase 4 UX work, so it was fixed at the generator level rather than patched per-page:

- `inferFieldType` now resolves `$ref` and reads the real OpenAPI shape: `boolean` → checkbox, `integer`/`number` → number input (`z.coerce.number()`), a string `enum` → `<select>` (`z.enum([...])`), `array` of strings/enums → a comma-separated "tags" input that preprocesses back into `string[]`/enum-array, and a free-form `object` (or array-of-object, e.g. `Form.fields`) → a JSON textarea that preprocesses into `Record<string, unknown>` (or `Record<string, unknown>[]`).
- `Form.tsx`'s `FieldConfig` gained `'number' | 'checkbox' | 'select' | 'tags' | 'json'` types (plus an `options` list for select/tags) and renders each with the right control; its `schema` prop type was loosened from `ZodSchema<T>` (input pinned to `T`) to `ZodType<T, ZodTypeDef, any>`, since `z.preprocess`-based fields (tags/json) legitimately have a different pre-transform input type than their output.
- A required array-of-nested-object field (only two exist site-wide: `CreateFormInput.fields`, `ImportContactsInput.contacts`) has no generic Zod reproduction of the target's exact shape — the generator casts the mutation call `as any` at the boundary for just those pages rather than pretending a generic JSON textarea can prove out a typed array of `FormFieldInput`/`CreateContactInput`. Both are also getting fully hand-built UI (Form builder, contact import) later, so the cast is a transient stopgap, not a real solution.
- Two path-param bugs were fixed alongside the typing gap: `create-form` pages nested under a parent resource (e.g. `POST /campaigns/{campaignId}/deployments`) now capture and merge the path param into the submitted body, same as `edit-form` already did; and a new `action` pattern was added for bare confirm-actions (`send`/`pause`/`resume`/`end`/`duplicate`/`publish` — path param, no request body), since their mutation hooks take the id directly (`mutationFn: async (campaignId: string) => ...`), not a wrapped object — the old generic `create-form` codegen passed `{}` and failed to typecheck for exactly these ~8 pages.
- **A separate, more severe bug was found while wiring navigation for the Landing Page vertical slice (below), not by typecheck:** `genAppTsx` registered a create-form and its sibling list (and an edit-form and its sibling detail) as two `<Route>`s at the *identical* path (e.g. both `LandingPagesPage` and `CreateLandingPage` at `/landing-pages`). React Router silently renders only the first of two same-path siblings, so the second was permanently unreachable by URL — invisible to `tsc`, only visible by actually clicking through. Fixed generically: `create-form` routes now get a `/new` suffix, `edit-form` routes get `/edit` (e.g. `/landing-pages/new`, `/landing-pages/:landingPageId/edit`), applied across the whole app, not just landing pages. `genAppTsx`'s index/catch-all default was also changed from "first route in spec order" (previously hand-patched to `/home` once, per the deviation below) to "prefer a route literally named `/home` if one exists" — a generic rule so this doesn't need re-patching by hand every regeneration. All 76 pages and `App.tsx` were regenerated; typecheck stayed clean (still just the one `UpdateDeploymentPage` gap above).

All 76 stub pages were deleted and regenerated with the fixed generator (none had been hand-customized yet — Phase 4 hadn't started). `apps/web` now typechecks clean except for **one** pre-existing, separately-tracked gap: `UpdateDeploymentPage.tsx`, because no standalone `GET /deployments/{id}` operation exists in the API (see the `useDeployment` gap noted throughout this doc) — the generator can't invent an endpoint that isn't in the spec. Left open; not part of this fix's scope and not on the critical path of the Landing Page vertical slice that follows.

Two earlier *generator* bugs remain fixed from before, unrelated to this pass: `detectPattern` misclassified parameterless singleton GETs (`/home`, `/results`) as collection-list pages that called `.map()` on a plain object; `genDetailPage` handles the no-path-param case (no `useParams`, hook called with no args) instead of assuming every detail page has an id.

## Automation Execution (2026-08-27)

Automations were pure CRUD until now — stored, listed, pausable, but nothing ever ran them (`AutomationService.create()` had an explicit comment flagging this; `DashboardService.home()` had two literal placeholders, `followUpsDue` approximating as "count of active rules" and `automationErrors` hardcoded to `0`). Both are now real. Backend-only change by design — no new OpenAPI operations, no SDK/frontend surface; `AutomationLog` (already existed, already matched `docs/features/06-automation-rules.md`'s logging spec) stays the one user-visible execution history via the existing `GET /automations/{id}/logs`.

**Design: event-sourced scheduling, polling-based execution.** Not pure polling for everything — two of the four real triggers (status-change, sale-recorded) can't be reliably detected by scanning row state (`Lead.updatedAt` changes on *any* edit, not just a stage change) but can be captured precisely at the exact call site that already exists and already writes the relevant `Interaction` row. Polling is used only for "is this due yet," which is naturally poll-shaped (wait N days, check a condition).

- **`apps/server/src/lib/automationScheduling.ts`** — `scheduleAutomationRuns()`, called from the four real trigger points: `identityResolution.ts`'s `insertOrReuseOpenLead` (`LEAD_CREATED`, only on genuine creation, not a reused open lead), `MessageService.send()` (`MESSAGE_SENT`, per recipient), `LeadService.update()` (`LEAD_STATUS_CHANGED`, right after the existing `STATUS_CHANGE` interaction write), `SaleService.create()` (`SALE_RECORDED`, right after the existing `SALE_RECORDED` interaction write). Creates a `PENDING` `AutomationRun` per matching active `Automation`; idempotent via `@@unique([automationId, triggerSourceId])` — a duplicate call (e.g. a retried request) is a safe no-op, verified in `automationExecution.test.ts`.
- **`AutomationRun`** (new model) — the scheduling record: `runAt` (`triggerEventAt` + `waitDays`), `status` (`PENDING`/`EXECUTED`/`SKIPPED`/`CANCELED`). Internal execution plumbing, not exposed via the API.
- **`apps/server/src/services/AutomationExecutorService.ts`** — `runDueAutomations()`, the pollable entrypoint. For each due `PENDING` run: checks **default stop conditions** (always, regardless of the automation's own `condition` — per the spec's separate "Default Stop Conditions" section: paused, contact opted out on the action's channel, a reply since the trigger, a sale since the trigger, the linked lead closed since the trigger), then the **rule-specific condition** if one is set (`CUSTOMER_STATUS` is interpreted as "contact has ≥1 Sale" — no deeper spec exists for that term, a documented judgment call), then fires the **action** (`SEND_EMAIL`/`SEND_TEXT` write an `Interaction` at the same fidelity as `MessageService.send()` — no live provider exists, this records the real event exactly like every other "send" in this app, not a regression; `CREATE_REMINDER` writes a `NOTE`-type `Interaction`, no new Reminder model; `CHANGE_LEAD_STATUS` updates `lead.stage`; `NOTIFY_USER` is the `AutomationLog` entry itself, no notification/inbox model exists; `STOP_SEQUENCE` cancels the contact's other `PENDING` runs). One bad run can't kill the tick — failures are caught, logged as `AutomationLog(outcome: FAILED)`, and counted, not thrown.
- **Poller wiring** (`apps/server/src/index.ts`) — a plain `setInterval` (default 60s, `AUTOMATION_POLL_INTERVAL_MS` to override), guarded off in `NODE_ENV=test`. No queue/worker infra exists anywhere in this repo, so this matches the existing single-process Railway deployment rather than introducing new infrastructure for it.

**Explicit scope boundary — 2 of 6 triggers are not wired**: `CONTACT_REPLIES` has no data source (`Interaction.type = REPLY` is only ever read, never written, anywhere in the codebase — no inbound webhook exists) and `DATE_REACHED` is under-specified (date relative to what? no spec guidance). Automations using either trigger remain valid, inspectable, and pausable — they just never produce a run. Not a bug; a stated boundary, same treatment as the `useDeployment` gap elsewhere in this doc.

## Affiliate Attribution + Payouts (2026-08-27)

**Affiliate → referral link/code → tracked session → Contact/Lead → Sale → Commission → Payout**, with two hard constraints honored throughout: affiliate attribution stays separate from acquisition `sourceType`, and all money owed/paid flows through the existing `FinanceService` — no new affiliate-balance concept anywhere. The financial half of this already existed before this pass: `Commission`/`Payout`/`PayoutItem` models, an `AFFILIATE_PAYABLE` ledger account kind, and `FinanceService.createCommission`/`markCommissionPayable`/`createPayout` were all built by the concurrent finance session (`docs/architecture/00-unified-data-model.md` states outright they were built "for future affiliate support") — **zero changes were made to any of it.** What this pass added is everything upstream of a Commission: an `Affiliate` model, referral-link tracking, and the one hook that produces a Commission automatically when a referred lead converts.

**Backend only, no frontend pages this pass** — same scope choice the finance session itself made for Money & Ledger.

- **Separation, not overloading**: a `Lead`'s `sourceType` (`MESSAGE`/`DEPLOYMENT`/`AD_UNIT`/`MANUAL`/`IMPORT`) keeps meaning exactly what it always meant — how the lead reached the business — completely unaffected by affiliate referral. `Lead.referringAffiliateId` (new, plain field, not part of the `sourceType`/`source*Id` group) answers the orthogonal question "who gets credit," stamped once at Lead creation only (same "first touch, not retroactive" rule already used for the `LEAD_CREATED` automation trigger). A pure affiliate referral (no ad `Deployment`/`AdUnit` involved) resolves with `sourceType: MANUAL` — the exact same fallback the acquisition chain already uses for any organic, non-ad-tracked submission — proven explicitly in `affiliateAttribution.test.ts`.
- **One non-invasive hook, not a parallel identity system**: both existing submission paths (`AttributionService.submitForm`, `LandingPageService.submit`) already resolve a verified session id and pass it to the one shared `resolveContactAndLead()` (`apps/server/src/lib/identityResolution.ts`) as `attribution.landingSessionId`, in every case including the no-ad-event fallback. The only new code lives inside that one function: after a genuinely new Lead is created, look up the most recent `AffiliateReferralClick` for that session id and stamp `referringAffiliateId` if found. Neither submission handler needed to change at all.
- **`GET /r/affiliate/{affiliateId}`** (`AttributionService.trackAffiliateClick`, public) — mirrors `GET /r/{deploymentId}` exactly, reusing its `clickRedirectUrl()`/`withSid()` helpers unmodified: mints/reuses a signed session id, writes an `AffiliateReferralClick` (a separate table from `AttributionEvent` — an affiliate isn't a paid ad platform, no reason to overload that model's campaign/deployment/ad-unit shape), redirects to the affiliate's destination landing page or fallback URL.
- **`SaleService.create()`** — one addition: if the sale's linked Lead has a `referringAffiliateId`, compute `amountMinor = round(sale.amount * 100 * affiliate.commissionRateBps / 10000)` and call `financeService.createCommission(...)` with `payeeRef: "affiliate:${affiliate.id}"`, `sourceRef: sale.id`, and a deterministic `idempotencyKey` (`commission:sale:${sale.id}` — a retried request can't double-commission). Same post-commit, best-effort convention already used there for `SALE_RECORDED` automation scheduling. `Commission.payeeRef`/`sourceRef` are plain free-form strings, not relations — precisely so this works without the finance schema ever needing to know what an "affiliate" is.
- **`commissionRateBps` is required per-affiliate, no platform-wide default** — a business must consciously set a rate; documented judgment call, no spec existed for this.
- **`AffiliateService`** — CRUD (list/create/get/update/pause/resume), mirrors `AutomationService`'s exact shape. `referralCode` is globally unique (like `LandingPage.slug` — same reasoning: a clean public link, no business-id prefix), auto-generated with a retry-on-collision loop when omitted, explicit codes get a direct 409 like `LandingPageService.create`'s slug handling.
- **`Lead`'s response schema gained `referringAffiliateId`** (nullable, read-only) — the only change to an existing public API shape; no other endpoint, DTO, or finance route was touched.

## Shared Database Policy (2026-08-27)

**The shared dev database (`loopie`) is no longer disposable test state.** It used to be safe to wipe (`setup.ts`'s global `afterEach` deletes every business/contact/lead/... row) because this repo held only this session's own work. That's no longer true: this same working tree also holds concurrent, independently-owned work — a full finance/ledger subsystem and public/auth hardening — landed by a parallel session on the same project. Discovered mid-session when `apps/server/src/__tests__/helpers/setup.ts` turned out to contain 11 lines of finance-table cleanup this session never wrote, and the repo (which had no `.git` at session start) turned out to already have two real commits. Confirmed with the user: expected, not a collision — the finance/ledger and hardening work stays exactly as landed, untouched.

Going forward: **tests never run against whatever `DATABASE_URL` happens to be exported in the shell.** `apps/server/vitest.config.ts` and `apps/ad-server/vitest.config.ts` both set `test.env.DATABASE_URL` to a dedicated `loopie_test` database by default (override with `TEST_DATABASE_URL`), so `pnpm --filter server test` / `pnpm --filter ad-server test` are safe to run at any time without coordinating with whoever else is using the shared `loopie` DB. `loopie_test` needed its own MySQL grant — the `loopie` user's privileges are scoped to exactly the `loopie` database, so creating it required a one-time admin command (`CREATE DATABASE loopie_test; GRANT ALL PRIVILEGES ON loopie_test.* TO 'loopie'@'localhost';`) the user ran themselves. Schema is kept in sync there the same way as the real DB (`pnpm db:push` with `DATABASE_URL` pointed at it) — verified purely additive via `prisma migrate diff --from-empty` before pushing (all `CREATE TABLE`, zero `DROP`, all 42 models including the finance/security ones represented).

**One casualty found, not caused, by this discovery**: the shared dev DB's seeded demo data (`Riverside Auto Detailing` etc.) was already gone by the time this was investigated — almost certainly wiped by the finance suite's own test run before its `vitest.config.ts` had a test-DB override (it shares the same global-wipe `setup.ts`). Restored via `pnpm db:seed`. The same `vitest.config.ts` fix now protects those tests too, not just this session's.

## Live Database: Verified

A real `DATABASE_URL` (`mysql://loopie:loopie_dev_password@localhost:3306/loopie` — a pre-provisioned MariaDB user/db on this machine, not created by this session) replaced the earlier "no provisioning access" blocker. Full path exercised for real:

- `pnpm db:push` — the entire 30-model schema, including the LandingPage↔PublishedPageVersion cycle, synced clean on the first try.
- `pnpm db:seed` — creates a full realistic dataset (business, contact, lead, message, campaign, deployment, landing page + published version, form, ad unit) with no errors.
- `pnpm --filter server test` and `pnpm --filter ad-server test`, both against the live DB. The two hand-written integration suites with real assertions — `acquisitionPath.test.ts` (2 tests) and `adServe.test.ts` (4 tests) — **pass 100%**. The 91 auto-generated stub tests are ~59% passing; every remaining failure is a plain 404 (placeholder UUID path param, no seeded resource) or 500 (payload deliberately left as `// payload: {},`, never filled in) — exactly the documented, expected state of unfilled stubs (see `references/testing-patterns.md` — filling them in is explicitly a separate developer task, not something this pass was asked to do).
- Manual curl smoke test end-to-end: login → session cookie → `/home` and `/results` (correctly blending Deployment + AdUnit impressions: 42,100 + 3,200 = 45,300) → hosted `/p/raw-customer-stories` renders real HTML with the real form → tracked click at `/r/demo-deployment-meta` redirects correctly → form submission creates a real attributed Contact + Lead → `/results` and campaign `/performance` both update correctly afterward.

**One real bug found and fixed by this verification, not by inspection:** the click redirect (`AttributionService.trackClick` in `apps/server`, `AdServeService.recordClick` in `apps/ad-server`) never appended the session id to the redirect URL, and the rendered landing page's form-submit script referenced an undefined `window.__loopieSid` instead of reading `sid` from its own URL. Manually querying the DB for the session id made the flow *look* correct in isolation, but a real browser visitor had no way to know it — the click→page→submission chain was silently unlinked in practice. Fixed in both services (`withSid()` helper appends `?sid=` to any absolute redirect URL) and in `lib/renderLandingPage.ts` (form script now reads `sid` from `location.search`). Both integration test suites were strengthened to assert this explicitly (parsing the actual redirect `Location` header rather than reading the session id back out of the database) and still pass.

**One operational note:** the test suites' `afterEach` wipes every table in the connected database (by design, per `references/testing-patterns.md`) — running them against the same DB used for seeded demo data destroys that data. Re-run `pnpm db:seed` after running tests if you need the demo data back; a separate test database is the real fix if this becomes a recurring workflow.

## Deviations from Defaults

- **OpenAPI version pinned to 3.0.3, not 3.1.0.** See prior reasoning — `nullable: true` throughout is invalid OpenAPI 3.1.
- **SDK hooks never destructure `{ data, error, response }` off an awaited `client.METHOD(...)` call, and never narrow directly on `result.error` inside an `if`.** TS 5.9.3 + openapi-fetch 0.12.5 collapses inference to `never` otherwise — verified in isolation. See `packages/sdk/src/hooks/useAuth.ts`.
- **`RegisterInput` uses `businessName`, not `username`/`displayName`.**
- **Business/User split instead of User/Profile split.**
- **Source is a discriminant (`sourceType`) + typed nullable FKs** — now three: `sourceMessageId`, `sourceDeploymentId`, `sourceAdUnitId`. Same reasoning as before, extended.
- **`apps/ad-server` does not use `fastify-openapi-glue` / the shared OpenAPI spec.** Its four routes are public, high-volume, and consumed by embed snippets and tracking pixels, not the LOOPIE web app's typed SDK — a second contract-first pipeline for four routes would be machinery without a benefiting consumer. See `apps/ad-server/README.md`.
- **`AttributionEvent.deploymentId`/`adUnitId`/`campaignId`/`creativeId`/`platform` are all nullable now** (were required). Either `deploymentId` xor `adUnitId` is set, enforced in application code (both `AttributionService.trackClick` and `AdServeService.recordClick`), not a DB constraint — Prisma/MySQL has no clean way to express "exactly one of two nullable FKs" declaratively.
- **`LandingPage.slug` is globally unique**, not per-business — supports clean `/p/{slug}` hosted URLs without a business-id prefix, and sets up custom-domain mapping later. Enforced at the service layer (409 on conflict) and via `@unique` in schema.
- **`Form` structure is not frozen into `PublishedPageVersion`.** Only `content`/`theme`/`formId` (a pointer) are snapshotted on publish. Editing a Form's fields after publishing changes what a live page renders. This is intentional — Forms are reusable, live entities by design (see the brief: "first-class reusable entities rather than embedded JSON") — but it means "immutable published version" covers page content, not form structure. Documented in `schema.prisma` and the `updateForm` OpenAPI description.
- **Landing-page performance's `leads`/`sales` are computed by joining through `FormSubmission.leadId`**, not a direct `landingPageId` FK on `Lead`/`Sale`. Keeps the Source pattern exactly as it was (no new FK sprawl on the core spine) at the cost of an extra join — acceptable at V1 scale.
- **AdUnit has no `spend` field.** First-party inventory has no external-platform cost; `byPlatform`/`bySource` rows for `LOOPIE`/`AD_UNIT` always report `spend: null`/`0`.
- **`packages/db/tsconfig.json`** now includes `prisma` (previously only `src`), and `bcryptjs`/`@types/bcryptjs` moved to `packages/db`'s own dependencies (were only in `apps/server`'s) — `prisma/seed.ts` uses bcrypt directly and was silently unchecked before this pass.
- **`apps/server/vitest.config.ts` sets `fileParallelism: false`.** Found live: Vitest's default parallel-file execution against one shared real database, combined with `setup.ts`'s global `afterEach` table wipe, caused test files to delete each other's mid-test data — surfaced as spurious 401s that had nothing to do with auth. Serial file execution fixed it; the underlying cause (one shared DB, no per-file isolation) is worth revisiting if the suite grows large enough for serial execution to matter.
- **Click redirects append `?sid=<sessionId>`** to any absolute destination URL — a `withSid()` helper in `apps/server/src/services/AttributionService.ts`, duplicated (not shared, different packages) in `apps/ad-server/src/services/AdServeService.ts`. Found live, not by inspection — see "Live Database: Verified" above.
- **`apps/web` was scaffolded by hand, not `pnpm create vite`.** `pnpm dlx` failed with a persistent `ENOENT` on the pnpm dlx cache in this environment (survived a cache clear). Every file `create-vite` would have generated was instead copied from the skill's own templates (`templates/web/*`, `templates/configs/*`) — the templates-first path the skill already prefers, so no functional gap versus the scripted path.
- **`Shell.tsx`'s Home nav item points at `/home`, not `/`,** and `App.tsx`'s index/catch-all routes redirect to `/home`, not the alphabetically-first generated route (`/contacts`). `scripts/generate-pages.ts` has no way to know which page is meant to be the app's landing screen — it defaults to the first route it encounters — so this needed a manual fix to match `docs/00-unified-ia-navigation.md`'s actual IA (Home is the landing screen). Caught by the Playwright login test failing on `/contacts` instead of `/home`.
- **Landing Pages/Forms/Ad Units are not in the top-level Shell nav** — `docs/architecture/00-unified-ia-navigation.md` places them under Campaign detail (Forms still have no dedicated surface). `Shell.tsx` stays Home · Campaigns · Messages. **Resolved 2026-08-26**: Campaign detail sub-nav is Overview → Budget → Creatives → Ad Units → Leads. Landing Pages remain reachable from Overview destination + `/landing-pages`.
- **IA revised 2026-08-27: nav dropped from 5 top-level items to 3 (`Home · Campaigns · Messages`), reversing the earlier `Advertising` label back to `Campaigns` and demoting Contacts and Results/Reports out of the top nav entirely.** Contacts now lives under `Messages → Contacts`; there is no blended "Results"/"Reports" page — each surface gets its own `Performance` section instead (`Campaigns → Performance`, `Messages → Performance`), with Home staying the one operational landing screen. Explicit user preference, not a technical constraint — full rationale in `docs/00-unified-ia-navigation.md`'s "Revision (2026-08-27)" section. `Shell.tsx` was updated to match; the 76 already-generated routes (`/contacts`, `/campaigns`, etc. from `pages:generate`) did not change — only which ones the Shell links to directly.
- **A generated create-form page's post-success `navigate(-1)` is fragile — found live, not by inspection, while building the Landing Page slice.** It depends on the SPA's in-memory history stack; reach the page by a direct URL (typed, bookmarked, or a Playwright `page.goto()`) rather than clicking a `<Link>`, and "back" falls through to a real browser navigation that reloads the wrong page instead of an SPA one. Fixed in `genCreateFormPage`: a top-level create (no parent path param) whose response is a single resource with an `id` now navigates straight to `${path}/${result.data.id}` — its own new detail page — instead of `navigate(-1)`. A nested create (e.g. a deployment under a campaign) or a non-resource bulk response (`ImportContactsInput` → `{created, skipped}`, no `id`) still uses `navigate(-1)`, since neither has a standalone detail page to land on.
- **`useCampaigns.ts`'s hand-written `CreateCampaignInput.platforms` type had drifted from the OpenAPI spec** — it excluded `'LOOPIE'`, even though `openapi.yaml` has allowed it since the ad-serving extension (selecting `LOOPIE` as a campaign platform creates first-party `AdUnit` rows instead of a `Deployment` — see the spec's own description on that field). Nothing had ever round-tripped a real `platforms` array through a correctly-typed form before, so the drift was invisible until `CreateCampaignPage.tsx` — now genuinely enum-typed post-Known-Gap-fix — tried to submit one. Fixed by widening the local type to match the spec (contract-first: the spec is the source of truth, hand-written SDK types should mirror it, not diverge).
- **`packages/db/src/signedSid.ts`'s `issueSid` had no explicit parameter type** (`sessionId = randomUUID()`) — TS infers the parameter's type from the default value, and `randomUUID()`'s return type is a UUID template-literal type, not plain `string`, so passing any other `string` (e.g. from `verifySid()`) failed to typecheck. Pre-existing, unrelated to this session's other work; surfaced only because this was the first time `pnpm typecheck` was run across every package rather than just `apps/web`. Fixed with an explicit `sessionId: string` annotation.
- **Money & Ledger (2026-08-26): no stored balances.** `FinancialAccount` has no balance field; `LedgerTransaction`/`LedgerEntry` have no `updatedAt` and are never edited after post. `Campaign.budget` stays a Decimal planning field; available funds come from `CLIENT_AD_FUNDS` ledger balance, reserved funds from `CLIENT_FUNDS_RESERVED` entries scoped by `campaignId`, and platform-reported vs settled spend live as separate columns on `AdSpend` (reconciliation persists a comparison row rather than overwriting either). Integer `amountMinor` + ISO currency only. All ledger writes go through `FinanceService` / `lib/finance/ledger.ts#postLedger` inside Prisma `$transaction`. Frontend finance pages were not generated this pass (`pnpm pages:generate` not run) — contract, service, SDK hooks, and integration tests only.

## Live-DB Testing Process Note

This is a **shared machine running other people's projects** — mid-session, `pnpm --filter web test:e2e` reused a stray dev server on port 3001 (`reuseExistingServer: true`) that turned out to belong to an unrelated project (`codeschooled`), not LOOPIE, causing two confusing false-failure test results before this was diagnosed. Separately, several of this session's own broad `pkill -f "<pattern>"` cleanup calls silently matched nothing (the pattern didn't account for `tsx`'s actual multi-segment invoked command line), leaving stray LOOPIE dev-server processes running for the rest of the session undetected. Neither caused any actual damage here (verified: `codeschooled`'s processes were never touched; the one non-LOOPIE, non-`codeschooled` process a broad `pkill -f "vite"` could plausibly have matched was independently confirmed still running afterward) — but both are exactly the kind of shared-machine hazard worth remembering: **check `ss -ltnp` / `ps aux` for what's *actually* on a port before assuming a service check failure is your own code's fault, and kill dev-server processes by exact PID once identified, not by broad pattern.**

**Recurred 2026-08-27, same root cause, different port**: port 3001 was `codeschooled` again; ran `apps/server`'s dev process manually on `PORT=3021` instead (confirmed free first) with `VITE_API_URL=http://localhost:3021` for the web dev server, and `PLAYWRIGHT_API_URL=http://localhost:3021` when driving Playwright, so `reuseExistingServer` would reuse the already-running pair instead of touching 3001. One new wrinkle: **`apps/server`'s `dev` script (`tsx watch src/index.ts`) does not load any `.env` file itself** — it only sees whatever's already exported in the invoking shell. `pnpm db:seed`/`db:push` worked fine without an explicit export because the Prisma CLI they call loads `packages/db`'s own `.env` automatically; the server has no equivalent, so `DATABASE_URL` must be exported (or passed inline) explicitly before `pnpm --filter server dev` — omitting it doesn't fail fast, it starts cleanly and only 500s on the first DB-touching request, which reads as a app bug for a moment until the actual cause (no env file loaded) is spotted in the stack trace's Prisma error.

## Last Session Summary

Two large pieces of work landed back-to-back. First, extended the architecture per an explicit brief to add the missing acquisition layer: Landing Pages, Forms, and first-party ad serving, completing the chain `Campaign → Creative → Deployment/AdUnit → LandingPage → Form → Submission → Contact → Lead → Message/Automation → Sale`, following the brief's 10-step order (schema → OpenAPI/SDK → new entities → submission integration → performance aggregation → `apps/ad-server` scaffold → tests). Key decisions honored: AdUnit stays fully separate from Deployment; `apps/ad-server` is a standalone process/Railway service, not a route group; identity resolution is one shared function called from both the pre-existing attribution flow and the new landing-page flow — no second Contact/Lead model anywhere. Caught and fixed a real latent bug in the original Phase 2 `setup.ts` (deleted `automation` before `message` despite a live FK reference).

Then, live-DB verification was requested and run for real against a pre-provisioned MariaDB instance: `db:push` (30-model schema, clean), `db:seed`, both hand-written integration suites (100% passing), and a full manual curl walkthrough of the acquisition chain. This surfaced — and fixed — a real bug no amount of code review had caught: the click redirect never carried the session id forward, so a real visitor's landing-page view/submission could never automatically link back to their click. It only "worked" in earlier testing because the session id was being pulled directly out of the database by hand. Also fixed: parallel Vitest file execution racing against a shared-DB global cleanup, which had been masquerading as spurious auth failures.

Then, in the same session, Phase 3 (frontend shell) was built and verified: full Vite/Tailwind/shadcn-pattern scaffold, all UI primitives, `Shell.tsx` with the real Home · Contacts · Messages · Advertising · Results nav, dark mode, 76 generated page stubs, and — pulled forward from Phase 4 because Phase 3's own gate required a working Home page — SDK domain hooks for every single resource in the spec. Fixed two real bugs in the shared `scripts/generate-pages.ts` generator itself (not just this project's stubs): parameterless singleton GETs like `/home` were misclassified as list pages and crashed calling `.map()` on a plain object; and the index/catch-all routes defaulted to the alphabetically-first generated route (`/contacts`) instead of the actual intended landing screen. Verified for real with Playwright against a live browser, live server, live database — 3/3 passing (login→Home→logout, register, unauthenticated redirect) — not just a typecheck pass, after first misdiagnosing a `codeschooled` port collision as a LOOPIE bug (see "Live-DB Testing Process Note" above).

After that, the top-level IA was revised again per explicit user direction: `Advertising` reverts to `Campaigns` as the literal label, and Contacts/Results drop out of the top nav (Contacts moves under `Messages → Contacts`; Results/Reports is dropped as a nav concept in favor of a `Performance` section within each of Campaigns and Messages). `docs/00-unified-ia-navigation.md` and `Shell.tsx` were updated to match; see the "IA revised 2026-08-27" deviation above for the full before/after and rationale.

Most recently (2026-08-26), completed the campaign story through outcomes: Overview → Budget → Creatives → Ad Units → Leads. Leads is campaign-scoped (contact, source, stage, attributed value, last interaction, follow-up) and click-through goes to Contact timeline. Playwright `e2e/campaign-leads.spec.ts` covers the slice.

In parallel (2026-08-27, separate concurrent session on the same working tree — see "Shared Database Policy" above), built automation execution: told to stop expanding the page-builder surface and take on the next product-critical runtime gap instead, so this landed entirely backend-side. Full design in "Automation Execution" above — event-sourced scheduling from the four real trigger call sites, a polling executor for the wait/condition/action evaluation, `DashboardService.home()`'s two automation placeholders now real. Discovering the finance/hardening work mid-task (a file about to be edited turned out to already contain 11 lines this session never wrote) changed the verification plan: rather than run tests against the shared `loopie` DB as every prior session had, a dedicated `loopie_test` database was provisioned and both `apps/server`/`apps/ad-server` `vitest.config.ts`s now point there by default. `automationExecution.test.ts` (5 tests, real assertions — a trigger firing an action, a default stop condition skipping it, a status-change action, scheduling idempotency, a paused automation still being caught at execution time) passes 100% there, alongside `acquisitionPath.test.ts` and `financeLedger.test.ts` run together in the same suite with no cross-test interference.

Then (2026-08-27, same concurrent-session pattern as automation execution — see "Shared Database Policy" above, re-confirmed with the user before starting): built Affiliate Attribution + Payouts, picked as the next Phase 4 feature specifically because the finance primitives already existed and the acquisition/automation spine was stable enough to support it. Full design in "Affiliate Attribution + Payouts" above — the entire feature is upstream of `Commission`; `FinanceService` itself was never touched. The two hard constraints from the brief (affiliate attribution separate from `sourceType`; all money through `FinanceService`, no new balance concept) shaped every design choice: one non-invasive hook inside the already-shared `resolveContactAndLead`, `Commission.payeeRef`/`sourceRef` used as loose free-form references rather than teaching the finance schema what an affiliate is. Backend only, matching how Money & Ledger itself shipped. `affiliateAttribution.test.ts` (2 tests — the separation proven explicitly, i.e. asserting `sourceType` stays `MANUAL` while `referringAffiliateId` gets stamped, plus a Sale auto-creating the right Commission through to a paid Payout) passes 100% against `loopie_test`, run together with the other three hand-written suites with no interference. Schema change was one `ADD COLUMN` + two `CREATE TABLE`, verified additive via `prisma migrate diff` before pushing, same discipline as the automation-execution pass; the shared `loopie` DB doesn't even have the new tables, confirmed directly.

Next: Phase 4 **Messages sub-nav** (Contacts/Audiences/Templates real pages). A live payment processor stays parking-lot. `useDeployment` gap still blocks `UpdateDeploymentPage.tsx`. `CONTACT_REPLIES`/`DATE_REACHED` automation triggers stay unwired (no data source / no spec depth — see "Automation Execution" above) until an inbound-reply capture mechanism exists. Affiliate frontend (portal/payee directory, referral-link display) is the natural next slice if wanted — the backend contract is ready.
